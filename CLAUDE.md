# 20 — animalcountllm

API นับสัตว์จากภาพกล้องความร้อน ตอบเป็น JSON: **ชนิดสัตว์ · จำนวน · ค่าความเชื่อมั่น 0-1**

repo: `https://github.com/boonruang/animalcountllm` · deploy: DigitalOcean App Platform
โครงการต้นทาง: [[projects/elephant-detection]] (POC ช้าง กับ [[people/suchart-senanon]])

---

## กติกาข้อแรก เราแค่ตอบคำถาม

**บริการนี้ไม่ยิงใคร ไม่เตือนใคร ไม่ตัดสินใจแทนใคร** รับภาพเข้ามา ตอบ JSON กลับไป จบ
edge box ที่เรียกเรา เป็นคนเอาผลไปตัดสินใจเอง

แปลว่าห้ามมีของพวกนี้ในโค้ด: outbound webhook · alert threshold · การส่ง LINE/email ·
คิวงานเบื้องหลังที่ยิงกลับไปหาปลายทาง

สิ่งที่เราทำแทนคือ **ตอบให้ครบพอที่ปลายทางจะตัดสินใจเองได้** ค่าดิบ ค่าที่กรองแล้ว
เหตุผลที่กรอง สภาพหน้าต่างข้อมูล ส่งไปให้หมด ไม่ยุบเป็นเลขเดียวลึกลับ

---

## สถาปัตยกรรม

```
app ฝั่งกล้อง → POST /v1/frames  {camera_id, ts, image_base64}
      │
      ├─[1] บันทึกดิบลง DB ทันที ก่อนทำอะไรทั้งสิ้น
      ├─[2] CV: normalize → แยกก้อนร้อน → bbox/area/aspect   (numpy+scipy ล้วน ไม่ใช้ GPU)
      │      └─ ไม่มีก้อนขนาดสัตว์เลย → ตอบ 0 ตัว ไม่ยิง LLM  ← ประหยัดเงินตรงนี้
      ├─[3] VLM: ชนิดสัตว์ + ยืนยันจำนวน   (LM Studio ตอน dev · OpenRouter ตอน prod)
      ├─[4] corroboration: conf < 0.60 → provisional รอเฟรมถัดไป (สูงสุด 2 เฟรม)
      ├─[5] filter: Hampel (median+MAD) + hysteresis เทียบหน้าต่าง 100 วิ ของกล้องตัวนั้น
      ├─[6] บันทึกผลดิบ + ผลกรอง + เหตุผล
      └─ response JSON
```

**จำนวนมาจาก CV ไม่ใช่จาก LLM** VLM นับของในภาพไม่คงเส้นคงวา ถามสองครั้งได้สองคำตอบ
ส่วน connected component นับได้เท่าเดิมทุกครั้ง หน้าที่ของ VLM คือ **บอกว่าก้อนนั้นคือสัตว์อะไร**
และยืนยัน/ยุบ/แยกก้อนที่ CV แบ่งผิด ไม่ใช่ตั้งต้นนับเอง

---

## endpoint

| method | path | ทำอะไร |
|---|---|---|
| POST | `/v1/frames` | เส้นหลัก รับ base64 ตอบผลวิเคราะห์ |
| GET | `/v1/frames/{id}` | ดูผลย้อนหลังรายเฟรม (option ตรวจย้อนหลัง) |
| GET | `/v1/cameras/{id}/window` | ดูหน้าต่าง 100 วิ ปัจจุบัน ใช้ debug filter |
| POST | `/v1/frames/{id}/truth` | บันทึก ground truth ที่คนตรวจแล้ว (option) |
| GET | `/healthz` | สถานะ + ต่อ LLM ได้ไหม |

`/truth` มีไว้เพื่อวันที่จะ calibrate confidence จริงๆ **ไม่มีข้อมูลนี้ = calibrate ไม่ได้ตลอดกาล**
เก็บไว้ตั้งแต่วันแรกเพราะมันถูกมาก และย้อนไปเก็บทีหลังไม่ได้

---

## response

```json
{
  "request_id": "uuid",
  "camera_id": "cam-01",
  "received_at": "2026-08-16T04:30:12+07:00",
  "status": "ok",

  "raw": {
    "detections": [
      {"id": 1, "bbox": [x,y,w,h], "area_px": 4210, "aspect": 1.42,
       "species": "elephant", "species_confidence": 0.72,
       "detection_confidence": 0.91}
    ],
    "counts": {"elephant": 3, "unknown": 1},
    "overall_confidence": 0.68
  },

  "filtered": {
    "counts": {"elephant": 3},
    "confidence": 0.74,
    "method": "hampel_median_mad + hysteresis",
    "accepted": true,
    "reason": "within 3.0 MAD of window median",
    "state": "rising"
  },

  "window": {
    "span_seconds": 100, "frames_used": 10, "frames_expected": 10,
    "complete": true,
    "median": {"elephant": 3}, "mad": {"elephant": 0.5}
  },

  "model": {"provider": "openrouter", "name": "qwen/qwen3-vl-...", "prompt_version": "v1"},
  "timing_ms": {"cv": 18, "vlm": 2400, "total": 2450}
}
```

`status` = `ok` · `provisional` (conf ต่ำ รอเฟรมถัดไป) · `degraded` (LLM ล่ม เหลือแต่ผล CV) · `error`
`state` = `stable` · `rising` · `falling` · `unstable` (เฟรมเห็นไม่ตรงกัน) · `unconfirmed`

**`raw` กับ `filtered` แยกกันเสมอ ไม่เคยทิ้งค่าดิบ** วันที่ filter ตัดของจริงทิ้ง
ต้องย้อนดูได้ว่ามันตัดอะไรและเพราะอะไร

---

## ค่าความเชื่อมั่น มีสองตัว ห้ามปนกัน

- **`detection_confidence`** มาจาก CV **วัดได้ อธิบายได้** (ก้อนชัด ขนาดสมเหตุผล ไม่ติดขอบเฟรม
  contrast กับพื้นหลังพอ) ทำซ้ำได้เป๊ะ
- **`species_confidence`** มาจาก VLM **โมเดลพ่นออกมาเอง ไม่ได้ calibrate** เลข 0.9 ของ LLM
  ไม่ได้แปลว่าถูก 90% ห้ามเอาไปคำนวณราวกับเป็นความน่าจะเป็นจริง

`overall_confidence` = `detection_confidence × species_confidence` ต่อก้อน แล้วถ่วงน้ำหนักด้วย area
**เขียนสูตรไว้ตรงนี้เพราะมันต้องอธิบายได้ ไม่ใช่เลขที่โผล่มาเฉยๆ**

---

## corroboration: เมื่อ confidence < 0.60

ตัดสินใจโดย Toy 2026-08-16

1. `species_confidence >= 0.60` → commit ทันที `status: "ok"`
2. `< 0.60` → `status: "provisional"` **ตอบกลับทันที ไม่ค้าง HTTP request**
   verdict ยังไม่ commit ปลายทางเห็นแล้วรู้ว่ารออีกเฟรม
3. เฟรมถัดไปมา (10 วิ) ถ้า species ตรงกัน → commit `corroborated_frames: 2`
4. **รอได้มากสุด 2 เฟรม (20 วินาที)** เกินนั้น commit ด้วย `state: "unconfirmed"`
   ปล่อยให้ปลายทางตัดสิน ไม่ค้างไม่มีกำหนด
5. เฟรมถัดไปเห็น**ไม่ตรงกัน** → `state: "unstable"` ส่งไปทั้งสองคำตอบ ไม่เลือกให้

**สูตรรวม confidence หลายเฟรม**

```
conf = median(c_i ของเฟรมที่เห็นตรงกัน) + 0.05 × (n_agree − 1)     เพดาน 0.85
```

**ห้ามใช้ `1 − Π(1−c)`** สูตรนั้นตั้งอยู่บนสมมติฐานว่าแต่ละครั้งเป็นอิสระต่อกัน ซึ่งไม่จริง:
เฟรมห่าง 10 วิ เป็นสัตว์ตัวเดิม มุมเดิม โมเดลเดิม พลาดรอบแรกก็พลาดแบบเดิมรอบสอง
สูตรนั้นจะยก 0.52+0.55 เป็น 0.78 ซึ่งเป็นตัวเลขปลอบใจตัวเอง

สูตรข้างบนเป็น **heuristic ไม่ใช่ความน่าจะเป็นที่ calibrate แล้ว** พอมี ground truth จาก
`/truth` พอสมควรค่อยแก้สูตรด้วยข้อมูลจริง จนกว่าจะถึงตอนนั้น อย่าอ้างว่ามันคือความน่าจะเป็น

**ต้นทุนของการรอ** 1 เฟรม = 10 วิ ช้างเดิน ~5 กม./ชม. = ขยับ **14 เมตร** · 2 เฟรม = 28 เมตร
นี่คือเหตุผลที่เพดานอยู่ที่ 2 เฟรม ไม่ใช่ 5

---

## หน้าต่าง 100 วินาที

คิด**เป็นเวลาเป็นหลัก ไม่ใช่จำนวนเฟรม** ปกติ 100 วิ = 10 เฟรมพอดี แต่ถ้ากล้องหลุดไป 3 นาที
แล้วกลับมา "10 เฟรมล่าสุด" จะกลายเป็นข้อมูลข้าม 4 นาที แล้วเอาอดีตมาตัดสินปัจจุบัน ซึ่งผิด

ดึงเฉพาะที่อยู่ใน 100 วิล่าสุด ได้เท่าไรใช้เท่านั้น แล้ว**บอกใน `window` ว่าใช้กี่เฟรม**
`complete: false` ปลายทางจะได้ลดน้ำหนักคำตอบเอง เหลือเฟรมเดียว = กรองไม่ได้ ต้องบอกตรงๆ
ไม่ใช่แกล้งกรอง

**หน้าต่างแยกตาม `camera_id` เสมอ** กล้องคนละจุด เอา median รวมกันไม่ได้

### filter: Hampel + hysteresis

- ใช้ **median + MAD ไม่ใช่ mean + SD** เพราะค่าเฉลี่ยถูกลากด้วยค่าผิดปกติที่เราพยายามจะตัดพอดี
  เฟรมเดียวที่ตอบว่าเห็นช้าง 40 ตัว จะทำให้ค่าเฉลี่ยเสียไปอีก 10 เฟรม median ไม่ขยับเลย
- 🔴 **filter ไม่สมมาตร fast attack, slow release**
  - **ขาขึ้น (0 → 3) ผ่านเร็ว** ยืนยัน 2 เฟรมพอ
  - **ขาลง (3 → 0) หน่วง** ต้องนิ่งหลายเฟรมก่อนประกาศว่าไม่มีแล้ว

  **เหตุผล:** ระบบเตือนภัยที่กรองค่ากระโดดออก จะกรองเหตุการณ์ที่มันมีไว้เพื่อจับทิ้งไปด้วย
  ช้างเดินเข้าเฟรม = จำนวนกระโดด 0 → 3 นั่นแหละคือเหตุการณ์จริง Hampel ตรงๆ จะบอกว่า
  "3 ห่างจาก median 0 เกินไป ตัดทิ้ง" แล้วเงียบในนาทีที่สำคัญที่สุด
  ต้นทุนเตือนผิด = คนรำคาญ · ต้นทุนไม่เตือน = ช้างถึงตัวคน สองอย่างนี้ไม่เท่ากัน filter ก็ไม่ควรเท่ากัน
- กรอง **species กับ count ไม่ใช่ confidence**
- **กรองหลังบันทึกค่าดิบเสมอ** ค่าดิบกับค่ากรองอยู่คนละคอลัมน์ ไม่ทับกัน

---

## เก็บข้อมูล

### 🔴 POC ใช้ทาง ค. — SQLite บน App Platform ยอมให้หายตอน deploy

ตัดสินใจโดย Toy 2026-08-16 · App Platform **ไม่มีดิสก์ถาวร** ทุก deploy/restart/scale
สร้าง container ใหม่แล้วทิ้งของเก่า ไฟล์ทุกอย่างหาย

**ผลที่ตามมา ต้องยอมรับ ไม่ใช่บั๊ก:**
- `animals.db` หายทุก deploy
- หน้าต่าง 100 วิ ว่างหลัง restart → 3-4 เฟรมแรกเป็น `provisional` หรือ `complete: false`
  ต้องตอบ `"reason": "cold start, window empty"` ตรงๆ ปลายทางจะได้ไม่งง
- ไม่มีประวัติข้ามรอบ deploy ระหว่าง POC

**ชั้นเก็บข้อมูลเขียนเป็น interface** สลับ SQLite ↔ Postgres ด้วย env ตัวเดียว
พอ POC จบแล้วอยากได้ของถาวร (Managed Postgres หรือ Droplet+Volume) เปลี่ยน env
ไม่ต้องรื้อโค้ดส่วนอื่น **อย่าเรียก sqlite3 ตรงๆ จากที่อื่นนอกชั้นนี้**

### ขนาด ไม่ต้องกังวล

8,640 เฟรม/วัน/กล้อง · 7 วัน = 60,480 แถว

| | ขนาด |
|---|---|
| เฟรมที่ CV ไม่เจออะไร (~90%) | ~300 B/แถว → 16 MB |
| เฟรมที่ยิง LLM (เก็บ response ดิบ) | ~1.2 KB/แถว → 7 MB |
| detections + index | ~7 MB |
| **รวมต่อสัปดาห์ต่อกล้อง** | **~30 MB** |

เขียน 0.1 ครั้ง/วินาที/กล้อง SQLite รับได้เป็นพันเท่า **เปิด WAL mode**
ลบแถวแล้วไฟล์ไม่เล็กลง มันเอาหน้าว่างกลับมาใช้ใหม่ ไฟล์จะนิ่งราว 30-40 MB **ไม่ต้อง VACUUM**

### ตาราง

- **`frames`** หนึ่งแถวต่อหนึ่งเฟรม: camera_id, ts, image_hash, image_path (ถ้าเก็บ), w, h,
  cv_result (JSON), llm_raw_response (ทั้งก้อน), prompt_version, model_name, provider,
  latency_ms, status, cost_usd
- **`detections`** หนึ่งแถวต่อหนึ่งตัวที่เจอ ผูก frame_id: species, count, confidence, bbox
- **`truth`** ground truth ที่คนตรวจ ผูก frame_id (option)
- **`daily_rollup`** สรุปวันละแถวต่อกล้อง

**index บน `(camera_id, ts)`** เพราะหน้าต่าง 100 วิ query ตัวนี้ทุก request

### `daily_rollup` ทำไมต้องมี

เก็บดิบ 7 วันแปลว่าเดือนหน้าตอบไม่ได้ว่า "เดือนที่แล้วกล้องนี้เจอช้างกี่ครั้ง"
ซึ่งเป็นคำถามที่โครงการนี้จะโดนถามแน่ๆ

สรุปตอนตี 1 ก่อนลบของดิบ: เจอกี่ครั้ง จำนวนสูงสุด ช่วงเวลา confidence เฉลี่ย
เฟรมทั้งหมด เฟรมที่ filter ตัด · **1 กล้อง 1 ปี = 365 แถว ไม่ถึง 100 KB**
ถูกจนไม่มีเหตุผลที่จะไม่ทำ และเป็นของที่ใช้ตอบผู้บริหารหรือเขียน paper ทีหลัง

### ภาพ

**ไม่เก็บ base64 ลง DB** 10 วิ/เฟรม = 8,640 ภาพ/วัน/กล้อง ราวใบละ 50 KB
เก็บทุกใบ = 3.5 GB/สัปดาห์/กล้อง

เก็บ **hash + metadata ทุกใบ** · เก็บ **ภาพจริงเฉพาะเฟรมที่เจอสัตว์ หรือเฟรมที่ filter ตัดทิ้ง**
(~10% = 350 MB/สัปดาห์) เฟรมที่โดนตัดต้องเก็บ **เพราะนั่นคือเฟรมที่จะอยากดูย้อนหลังที่สุด
ว่าตัดถูกหรือตัดพลาด** · บน App Platform ภาพก็หายตอน deploy เหมือนกัน POC ยอมรับได้

---

## LLM

| | dev | prod |
|---|---|---|
| provider | LM Studio `http://localhost:1234/v1` | OpenRouter |
| model | ตระกูล **Qwen** ตามที่ Toy กำหนด | qwen VL บน OpenRouter |

สลับด้วย env: `LLM_BASE_URL` `LLM_MODEL` `LLM_API_KEY` `LLM_PROVIDER`
ทั้งสองฝั่งเป็น OpenAI-compatible เลยใช้ `ChatOpenAI` ตัวเดียวกันได้

### วัดจริงแล้ว 2026-08-16 · qwen3.6-35b-a3b เห็นภาพได้

ผมเคยเขียนตรงนี้ว่ามันเป็น text-only **ซึ่งผิด** เดาจากชื่อรุ่นโดยไม่ทดสอบ
ทดสอบแบบเดาไม่ได้ (ก้อนสว่าง 7 ก้อน + รหัสสุ่ม `K7QZM`) ตอบถูกทั้งคู่ `blobs=7 code=K7QZM`

**บทเรียน: ยืนยันความสามารถด้วยภาพที่เดาไม่ได้ ไม่ใช่ด้วยชื่อรุ่น**
`tests/test_vision.py` ทำหน้าที่นี้ เรียกตอนสตาร์ตด้วย ถ้าอ่านรหัสไม่ออก = โมเดลไม่เห็นภาพ
ห้ามใช้ ต่อให้ชื่อจะมี VL ห้อยท้าย

### 🔴 ผลวัดที่กำหนดค่า max_tokens

| การตั้งค่า | เวลา/เฟรม | output | ผล |
|---|---|---|---|
| `max_tokens=200` | 10-11 วิ | 300 tok | ✅ ถูกเป๊ะ |
| `max_tokens=1500` | **37-39 วิ** | 1496 tok | ❌ คิดวนจนหลุดไปเขียนโค้ด cv2 ใส่ช่องคำตอบ |

**ยิ่งให้พื้นที่คิดเยอะ ยิ่งตอบแย่ลง** ไม่ใช่แค่ช้าลง · ความเร็วดิบ 40 tok/s ไม่ใช่ปัญหา
ปัญหาคือมันพ่น 1,500 token ทั้งที่คำตอบจริงยาว 20 token

เรื่องเดียวกับ [[projects/eval-harness/scaffold-agentic-finding]] · ปัญหาไม่ใช่โมเดลโง่
**ปัญหาคือมันไม่หยุด** โครงที่รัดแน่นเปลี่ยนโมเดลเดิมให้กลายเป็นคนละตัว

**กติกาที่ตามมา**
- `max_tokens = 200` เป็นค่าเริ่มต้น **ไม่ใช่กัน overflow แต่เพราะปล่อยยาวแล้วคำตอบแย่ลงจริง มีหลักฐาน**
- **บันทึก `finish_reason` ทุกครั้ง** ถ้า `length` = มันคิดจนหมดโควตา คำตอบเชื่อไม่ได้
  → ตี `degraded` ห้ามรับมาใช้เฉยๆ
- `enable_thinking: false` และ `/no_think` **ไม่มีผลกับโมเดลตัวนี้บน LM Studio** ลองแล้ว
  เวลาและ token เท่ากันเป๊ะ อย่าเสียเวลาลองซ้ำ

### เรื่องความละเอียด ข้อที่ยังไม่ทดสอบ

ภาพ 640×512 เข้าไปเป็น prompt **แค่ 430 token** = ถูกย่อลงเยอะก่อนเข้าโมเดล
ก้อนใหญ่ไม่มีปัญหา แต่ **ช้างที่ไกล 200 ม. กินพื้นที่ ~15×10 พิกเซล ย่อแล้วอาจหายไปเลย**

นี่คือเหตุผลเดียวที่มีน้ำหนักพอจะลอง Qwen3-VL: โมเดลสาย VL ทำ tiling ที่ความละเอียดเดิม
ได้ image token มากกว่าหลายเท่า + เทรนมาให้ตอบ bounding box ได้ ซึ่งเราต้องการเป็นรายก้อน

**ตัดสินด้วยตัวเลข ไม่ใช่ด้วยชื่อ** ยิงภาพชุดเดียวกันเข้าทั้งสองตัว วัด: ถูกกี่ครั้ง กี่วินาที ตรง schema ไหม

### LangChain ใช้แค่สองอย่าง

`ChatOpenAI` (สลับ base_url) + structured output ผูก Pydantic ให้ผลออกมาตาม schema แน่นอน
ไม่ต้อง parse ข้อความเอง

**ไม่ใช้ agent ไม่ใช้ memory ไม่ใช้ chain ซ้อน** งานนี้เป็น pipeline เส้นตรง
ยัด abstraction เกินจะ debug ยากเปล่าๆ

### ต้นทุน

8,640 เฟรม/วัน/กล้อง ยิง VLM ทุกเฟรมคือเงินจริงหลักพันบาท/เดือน/กล้อง
**CV เป็นด่านหน้า ไม่มีก้อนร้อนขนาดสัตว์ = ไม่ยิง LLM** ตอบ 0 ตัวไปตรงๆ
ซึ่งน่าจะเป็นกรณีส่วนใหญ่ ประหยัด >90% และเร็วกว่าด้วย

**timeout + fallback** เน็ตหลุด/LLM ช้า → ตอบ `degraded` พร้อมผล CV ไม่ค้างรอ

---

## ข้อจำกัดที่ต้องพูดตรงๆ ตั้งแต่วันแรก

**การระบุชนิดสัตว์จากภาพความร้อนล้วน มันยากกว่าที่ฟัง** ภาพ thermal ไม่มีสี ไม่มีลาย
เหลือแค่รูปทรงกับความต่างอุณหภูมิ ช้าง/ควาย/วัว ที่ระยะ 50 ม. ขึ้นไปคือก้อนขาวคล้ายกันหมด

ที่แยกได้จริงคือ **สัดส่วนรูปร่าง + ขนาดเชิงมุม** แต่ขนาดเชิงมุมแปลเป็นขนาดจริงไม่ได้ถ้าไม่รู้ระยะ
ต้องมีอย่างใดอย่างหนึ่ง: กล้องติดนิ่งแล้ว calibrate ระยะต่อพิกเซลของพื้นไว้ หรือมี RGB คู่กัน

ช้างพอมีลุ้นเพราะงวงกับหูเป็นเอกลักษณ์ในซิลูเอตต์ และตัวใหญ่สุดในฉาก
แต่ **confidence ที่ออกมาต้องสะท้อนความคลุมเครือนี้** ไม่ใช่ไปเจอตอนวัดผล
`unknown` ต้องมีอยู่ใน `counts` เสมอ ก้อนที่ระบุไม่ได้ไปอยู่ตรงนั้น
**ห้ามยัดเป็นช้างเพื่อให้ตัวเลขสวย**

---

## ที่ยังไม่รู้ ต้องถาม Toy / สุชาติ

- กล้องส่ง **radiometric 16-bit** (มีค่าอุณหภูมิจริงทุกพิกเซล) หรือ **JPEG/PNG ผ่าน palette สีแล้ว**
  → เปลี่ยนโค้ด CV ทั้งท่อน และเปลี่ยนว่าใช้เกณฑ์อุณหภูมิได้หรือไม่ได้
  ระหว่างรอ: ตรวจ bit depth เองตอนรับภาพ รองรับทั้งสองแบบ
- **กล้องติดนิ่งไหม** ถ้านิ่งทำ background model ได้ แม่นขึ้นเยอะ
- **แยกกี่ชนิด** ช้าง/สัตว์อื่น/คน พอ หรือต้องแยกวัว ควาย กวาง หมูป่า ยิ่งเยอะยิ่งพัง
- auth/rate limit ของ endpoint
- จะกลายเป็น paper ด้วยไหม ถ้าใช่ ต้องวางชุดทดสอบกับ ground truth ตั้งแต่แรก

---

---

## LangSmith

เปิดใช้เพราะ **ทาง ค. ทำให้ SQLite หายทุก deploy** LangSmith เลยกลายเป็นที่เก็บถาวร
ที่อยู่นอก container เป็นที่เดียวที่ log การเรียก LLM รอดข้ามรอบ deploy

| ข้อมูล | เก็บที่ | รอด deploy |
|---|---|---|
| ผลวิเคราะห์ที่ปลายทางใช้ · หน้าต่าง 100 วิ | SQLite | ไม่ (ยอมรับแล้ว) |
| prompt · response ดิบ · latency · cost · error | **LangSmith** | **ใช่** |

### 🔴 กติกาสามข้อ ตั้งผิดแล้วเสียเงิน

Toy ออกทุนเอง งบจำกัด ค่าเริ่มต้นทุกตัวต้องเลือกทางที่ถูกที่สุดที่ยังไม่เสียของ

1. **sampling ไม่ใช่เก็บทุกอัน** 860 trace/วัน/กล้อง = 26,000/เดือน ทะลุแพ็กฟรี (~5,000/เดือน)
   ตั้งแต่เดือนแรก กล้องเดียว
   - **เก็บ 100%** เฉพาะเฟรมที่ผิดปกติ: `conf < 0.60` · filter ตัดทิ้ง · `degraded` ·
     `finish_reason == "length"` · `state == "unstable"`
   - **เก็บ 5%** ของเฟรมที่ปกติดี (`LANGSMITH_SAMPLE_RATE`)
   - ของที่จะกลับมาดูจริงคือเฟรมที่มีปัญหา ไม่ใช่เฟรมที่ถูกอยู่แล้ว
2. **ห้ามส่งภาพ base64 เข้า trace** payload บวมและกินโควตา storage เร็วกว่ากินโควตา trace
   ส่งแค่ `image_hash` + ขนาด + ผล CV ที่เป็นตัวเลข อยากเห็นภาพค่อยแนบ thumbnail 128px
   ผลพลอยได้: ภาพจากไซต์อนุรักษ์ไม่ต้องไปนอนบนเซิร์ฟเวอร์เจ้าอื่น POC ไม่คิดมากก็ได้
   แต่ถ้ามีสัญญากับหน่วยงานทีหลังจะเป็นประเด็น ตั้งถูกตั้งแต่แรกง่ายกว่ามาแก้
3. **ปิดได้ด้วย env** `LANGSMITH_TRACING=false` ตอน test/CI/dev
   **เทสต์ต้องรันได้ตอนไม่มีเน็ต** ไม่งั้นรัน test 50 ครั้งกิน quota 50 trace เปล่าๆ

## stack

Python · FastAPI · LangChain (`ChatOpenAI` + structured output) · SQLite (interface สลับได้) ·
numpy + scipy + Pillow สำหรับ CV · **ไม่มี torch ไม่มี GPU ใน container**

## git

repo `boonruang/animalcountllm` · branch `main`
**ห้าม push เองจนกว่า Toy สั่ง**
