# 20 — animalcountllm

API นับสัตว์จากภาพกล้องความร้อน ตอบเป็น JSON: **ชนิดสัตว์ · จำนวน · ค่าความเชื่อมั่น 0-1**

repo: `https://github.com/boonruang/animalcountllm` · deploy: DigitalOcean App Platform
โครงการต้นทาง: [[projects/elephant-detection]] (POC ช้าง กับ [[people/suchart-senanon]])

---

## กติกาข้อแรก เราแค่ตอบคำถาม

**บริการนี้ไม่ยิงใคร ไม่เตือนใคร ไม่ตัดสินใจแทนใคร** รับภาพเข้ามา ตอบ JSON กลับไป จบ
edge box ที่เรียกเรา เป็นคนเอาผลไปตัดสินใจเอง

แปลว่าห้ามมีของพวกนี้ในโค้ด: outbound webhook · alert threshold · การส่ง LINE/email ·
คิวงานเบื้องหลังที่ยิงกลับไปหาปลายทาง

สิ่งที่เราทำแทนคือ **ตอบให้ครบพอที่ปลายทางจะตัดสินใจเองได้** ค่าดิบ ค่าที่กรองแล้ว
เหตุผลที่กรอง สภาพหน้าต่างข้อมูล ส่งไปให้หมด ไม่ยุบเป็นเลขเดียวลึกลับ

---

## สถาปัตยกรรม

```
app ฝั่งกล้อง → POST /v1/frames  {camera_id, ts, image_base64}
      │
      ├─[1] บันทึกดิบลง DB ทันที ก่อนทำอะไรทั้งสิ้น
      ├─[2] CV: normalize → แยกก้อนร้อน → bbox/area/aspect   (numpy+scipy ล้วน ไม่ใช้ GPU)
      │      └─ ไม่มีก้อนขนาดสัตว์เลย → ตอบ 0 ตัว ไม่ยิง LLM  ← ประหยัดเงินตรงนี้
      ├─[3] VLM: ชนิดสัตว์ + ยืนยันจำนวน   (LM Studio ตอน dev · OpenRouter ตอน prod)
      ├─[4] corroboration: conf < 0.60 → provisional รอเฟรมถัดไป (สูงสุด 2 เฟรม)
      ├─[5] filter: Hampel (median+MAD) + hysteresis เทียบหน้าต่าง 100 วิ ของกล้องตัวนั้น
      ├─[6] บันทึกผลดิบ + ผลกรอง + เหตุผล
      └─ response JSON
```

**จำนวนมาจาก CV ไม่ใช่จาก LLM** VLM นับของในภาพไม่คงเส้นคงวา ถามสองครั้งได้สองคำตอบ
ส่วน connected component นับได้เท่าเดิมทุกครั้ง หน้าที่ของ VLM คือ **บอกว่าก้อนนั้นคือสัตว์อะไร**
และยืนยัน/ยุบ/แยกก้อนที่ CV แบ่งผิด ไม่ใช่ตั้งต้นนับเอง

---

## endpoint

| method | path | ทำอะไร |
|---|---|---|
| POST | `/v1/frames` | เส้นหลัก รับ base64 ตอบผลวิเคราะห์ |
| GET | `/v1/frames/{id}` | ดูผลย้อนหลังรายเฟรม (option ตรวจย้อนหลัง) |
| GET | `/v1/cameras/{id}/window` | ดูหน้าต่าง 100 วิ ปัจจุบัน ใช้ debug filter |
| POST | `/v1/frames/{id}/truth` | บันทึก ground truth ที่คนตรวจแล้ว (option) |
| GET | `/healthz` | สถานะ + ต่อ LLM ได้ไหม |

`/truth` มีไว้เพื่อวันที่จะ calibrate confidence จริงๆ **ไม่มีข้อมูลนี้ = calibrate ไม่ได้ตลอดกาล**
เก็บไว้ตั้งแต่วันแรกเพราะมันถูกมาก และย้อนไปเก็บทีหลังไม่ได้

---

## `ui/` — หน้าเว็บทดสอบ · แยกจาก API เด็ดขาด

`ui/index.html` ไฟล์เดียว ไม่มี build step ไม่มี dependency ไม่มี framework
**โค้ดใน `app/` ไม่รู้จักมันเลย และห้ามรู้จัก** ไม่มี mount ไม่มี StaticFiles
ไม่มี CORS middleware · ถ้าวันไหนหน้าเว็บต้องการให้ API เปลี่ยน คำตอบคือแก้หน้าเว็บ

มันเป็นแค่ client ธรรมดาตัวหนึ่งที่ยิง `POST /v1/frames` เหมือน Postman
ผ่าน `_auth` เดิมทุกประการ ช่อง X-API-Key อยู่บนหน้า ไม่มีค่า default ไม่จำ

| | เสิร์ฟจากไหน |
|---|---|
| prod | คอมโพเนนต์ `static_sites: verify-ui` ใน `.do/app.yaml` ที่ path `/verify` |
| dev | `python ui/serve.py` แล้วเปิด `http://127.0.0.1:3000` |

🔴 **ทั้งสองทางต้องเป็น origin เดียวกับ API** เพราะ `app/main.py` ไม่มี CORS middleware
บน DO คือ app เดียวกัน คนละ route · ตอน dev `ui/serve.py` proxy `/v1/*` กับ `/healthz`
ไปที่ `127.0.0.1:8080` ให้ **เปิด `ui/index.html` จาก `file://` ตรงๆ ใช้ไม่ได้**
เบราว์เซอร์บล็อก fetch ทิ้งเงียบๆ ทั้งที่ API ตอบ 200 ปกติ ไล่บั๊กเสียเวลาเปล่า

ภาพอยู่ในหน่วยความจำแท็บเท่านั้น ไม่มี localStorage · base64 ถูกทิ้งหลังยิงเสร็จ
ฝั่งเซิร์ฟเวอร์ยังเก็บภาพตาม env `SAVE_IMAGES` เหมือนเดิม (`none` = ไม่เก็บเลย)

---

## response

```json
{
  "request_id": "uuid",
  "camera_id": "cam-01",
  "received_at": "2026-08-16T04:30:12+07:00",
  "status": "ok",

  "raw": {
    "detections": [
      {"id": 1, "bbox": [x,y,w,h], "area_px": 4210, "aspect": 1.42,
       "species": "elephant", "species_confidence": 0.72,
       "detection_confidence": 0.91}
    ],
    "counts": {"elephant": 3, "unknown": 1},
    "overall_confidence": 0.68
  },

  "filtered": {
    "counts": {"elephant": 3},
    "confidence": 0.74,
    "method": "hampel_median_mad + hysteresis",
    "accepted": true,
    "reason": "within 3.0 MAD of window median",
    "state": "rising"
  },

  "window": {
    "span_seconds": 100, "frames_used": 10, "frames_expected": 10,
    "complete": true,
    "median": {"elephant": 3}, "mad": {"elephant": 0.5}
  },

  "model": {"provider": "openrouter", "name": "qwen/qwen3-vl-...", "prompt_version": "v1"},
  "timing_ms": {"cv": 18, "vlm": 2400, "total": 2450}
}
```

`status` = `ok` · `provisional` (conf ต่ำ รอเฟรมถัดไป) · `degraded` (LLM ล่ม เหลือแต่ผล CV) · `error`
`state` = `stable` · `rising` · `falling` · `unstable` (เฟรมเห็นไม่ตรงกัน) · `unconfirmed`

**`raw` กับ `filtered` แยกกันเสมอ ไม่เคยทิ้งค่าดิบ** วันที่ filter ตัดของจริงทิ้ง
ต้องย้อนดูได้ว่ามันตัดอะไรและเพราะอะไร

---

## ค่าความเชื่อมั่น มีสองตัว ห้ามปนกัน

- **`detection_confidence`** มาจาก CV **วัดได้ อธิบายได้** (ก้อนชัด ขนาดสมเหตุผล ไม่ติดขอบเฟรม
  contrast กับพื้นหลังพอ) ทำซ้ำได้เป๊ะ
- **`species_confidence`** มาจาก VLM **โมเดลพ่นออกมาเอง ไม่ได้ calibrate** เลข 0.9 ของ LLM
  ไม่ได้แปลว่าถูก 90% ห้ามเอาไปคำนวณราวกับเป็นความน่าจะเป็นจริง

`overall_confidence` = `detection_confidence × species_confidence` ต่อก้อน แล้วถ่วงน้ำหนักด้วย area
**เขียนสูตรไว้ตรงนี้เพราะมันต้องอธิบายได้ ไม่ใช่เลขที่โผล่มาเฉยๆ**

---

## corroboration: เมื่อ confidence < 0.60

ตัดสินใจโดย Toy 2026-08-16

1. `species_confidence >= 0.60` → commit ทันที `status: "ok"`
2. `< 0.60` → `status: "provisional"` **ตอบกลับทันที ไม่ค้าง HTTP request**
   verdict ยังไม่ commit ปลายทางเห็นแล้วรู้ว่ารออีกเฟรม
3. เฟรมถัดไปมา (10 วิ) ถ้า species ตรงกัน → commit `corroborated_frames: 2`
4. **รอได้มากสุด 2 เฟรม (20 วินาที)** เกินนั้น commit ด้วย `state: "unconfirmed"`
   ปล่อยให้ปลายทางตัดสิน ไม่ค้างไม่มีกำหนด
5. เฟรมถัดไปเห็น**ไม่ตรงกัน** → `state: "unstable"` ส่งไปทั้งสองคำตอบ ไม่เลือกให้

**สูตรรวม confidence หลายเฟรม**

```
conf = median(c_i ของเฟรมที่เห็นตรงกัน) + 0.05 × (n_agree − 1)     เพดาน 0.85
```

**ห้ามใช้ `1 − Π(1−c)`** สูตรนั้นตั้งอยู่บนสมมติฐานว่าแต่ละครั้งเป็นอิสระต่อกัน ซึ่งไม่จริง:
เฟรมห่าง 10 วิ เป็นสัตว์ตัวเดิม มุมเดิม โมเดลเดิม พลาดรอบแรกก็พลาดแบบเดิมรอบสอง
สูตรนั้นจะยก 0.52+0.55 เป็น 0.78 ซึ่งเป็นตัวเลขปลอบใจตัวเอง

สูตรข้างบนเป็น **heuristic ไม่ใช่ความน่าจะเป็นที่ calibrate แล้ว** พอมี ground truth จาก
`/truth` พอสมควรค่อยแก้สูตรด้วยข้อมูลจริง จนกว่าจะถึงตอนนั้น อย่าอ้างว่ามันคือความน่าจะเป็น

**ต้นทุนของการรอ** 1 เฟรม = 10 วิ ช้างเดิน ~5 กม./ชม. = ขยับ **14 เมตร** · 2 เฟรม = 28 เมตร
นี่คือเหตุผลที่เพดานอยู่ที่ 2 เฟรม ไม่ใช่ 5

---

## หน้าต่าง 100 วินาที

คิด**เป็นเวลาเป็นหลัก ไม่ใช่จำนวนเฟรม** ปกติ 100 วิ = 10 เฟรมพอดี แต่ถ้ากล้องหลุดไป 3 นาที
แล้วกลับมา "10 เฟรมล่าสุด" จะกลายเป็นข้อมูลข้าม 4 นาที แล้วเอาอดีตมาตัดสินปัจจุบัน ซึ่งผิด

ดึงเฉพาะที่อยู่ใน 100 วิล่าสุด ได้เท่าไรใช้เท่านั้น แล้ว**บอกใน `window` ว่าใช้กี่เฟรม**
`complete: false` ปลายทางจะได้ลดน้ำหนักคำตอบเอง เหลือเฟรมเดียว = กรองไม่ได้ ต้องบอกตรงๆ
ไม่ใช่แกล้งกรอง

**หน้าต่างแยกตาม `camera_id` เสมอ** กล้องคนละจุด เอา median รวมกันไม่ได้

### filter: Hampel + hysteresis

- ใช้ **median + MAD ไม่ใช่ mean + SD** เพราะค่าเฉลี่ยถูกลากด้วยค่าผิดปกติที่เราพยายามจะตัดพอดี
  เฟรมเดียวที่ตอบว่าเห็นช้าง 40 ตัว จะทำให้ค่าเฉลี่ยเสียไปอีก 10 เฟรม median ไม่ขยับเลย
- 🔴 **filter ไม่สมมาตร fast attack, slow release**
  - **ขาขึ้น (0 → 3) ผ่านเร็ว** ยืนยัน 2 เฟรมพอ
  - **ขาลง (3 → 0) หน่วง** ต้องนิ่งหลายเฟรมก่อนประกาศว่าไม่มีแล้ว

  **เหตุผล:** ระบบเตือนภัยที่กรองค่ากระโดดออก จะกรองเหตุการณ์ที่มันมีไว้เพื่อจับทิ้งไปด้วย
  ช้างเดินเข้าเฟรม = จำนวนกระโดด 0 → 3 นั่นแหละคือเหตุการณ์จริง Hampel ตรงๆ จะบอกว่า
  "3 ห่างจาก median 0 เกินไป ตัดทิ้ง" แล้วเงียบในนาทีที่สำคัญที่สุด
  ต้นทุนเตือนผิด = คนรำคาญ · ต้นทุนไม่เตือน = ช้างถึงตัวคน สองอย่างนี้ไม่เท่ากัน filter ก็ไม่ควรเท่ากัน
- กรอง **species กับ count ไม่ใช่ confidence**
- **กรองหลังบันทึกค่าดิบเสมอ** ค่าดิบกับค่ากรองอยู่คนละคอลัมน์ ไม่ทับกัน

---

## เก็บข้อมูล

### 🔴 POC ใช้ทาง ค. — SQLite บน App Platform ยอมให้หายตอน deploy

ตัดสินใจโดย Toy 2026-08-16 · App Platform **ไม่มีดิสก์ถาวร** ทุก deploy/restart/scale
สร้าง container ใหม่แล้วทิ้งของเก่า ไฟล์ทุกอย่างหาย

**ผลที่ตามมา ต้องยอมรับ ไม่ใช่บั๊ก:**
- `animals.db` หายทุก deploy
- หน้าต่าง 100 วิ ว่างหลัง restart → 3-4 เฟรมแรกเป็น `provisional` หรือ `complete: false`
  ต้องตอบ `"reason": "cold start, window empty"` ตรงๆ ปลายทางจะได้ไม่งง
- ไม่มีประวัติข้ามรอบ deploy ระหว่าง POC

**ชั้นเก็บข้อมูลเขียนเป็น interface** สลับ SQLite ↔ Postgres ด้วย env ตัวเดียว
พอ POC จบแล้วอยากได้ของถาวร (Managed Postgres หรือ Droplet+Volume) เปลี่ยน env
ไม่ต้องรื้อโค้ดส่วนอื่น **อย่าเรียก sqlite3 ตรงๆ จากที่อื่นนอกชั้นนี้**

### ขนาด ไม่ต้องกังวล

8,640 เฟรม/วัน/กล้อง · 7 วัน = 60,480 แถว

| | ขนาด |
|---|---|
| เฟรมที่ CV ไม่เจออะไร (~90%) | ~300 B/แถว → 16 MB |
| เฟรมที่ยิง LLM (เก็บ response ดิบ) | ~1.2 KB/แถว → 7 MB |
| detections + index | ~7 MB |
| **รวมต่อสัปดาห์ต่อกล้อง** | **~30 MB** |

เขียน 0.1 ครั้ง/วินาที/กล้อง SQLite รับได้เป็นพันเท่า **เปิด WAL mode**
ลบแถวแล้วไฟล์ไม่เล็กลง มันเอาหน้าว่างกลับมาใช้ใหม่ ไฟล์จะนิ่งราว 30-40 MB **ไม่ต้อง VACUUM**

### ตาราง

- **`frames`** หนึ่งแถวต่อหนึ่งเฟรม: camera_id, ts, image_hash, image_path (ถ้าเก็บ), w, h,
  cv_result (JSON), llm_raw_response (ทั้งก้อน), prompt_version, model_name, provider,
  latency_ms, status, cost_usd
- **`detections`** หนึ่งแถวต่อหนึ่งตัวที่เจอ ผูก frame_id: species, count, confidence, bbox
- **`truth`** ground truth ที่คนตรวจ ผูก frame_id (option)
- **`daily_rollup`** สรุปวันละแถวต่อกล้อง

**index บน `(camera_id, ts)`** เพราะหน้าต่าง 100 วิ query ตัวนี้ทุก request

### `daily_rollup` ทำไมต้องมี

เก็บดิบ 7 วันแปลว่าเดือนหน้าตอบไม่ได้ว่า "เดือนที่แล้วกล้องนี้เจอช้างกี่ครั้ง"
ซึ่งเป็นคำถามที่โครงการนี้จะโดนถามแน่ๆ

สรุปตอนตี 1 ก่อนลบของดิบ: เจอกี่ครั้ง จำนวนสูงสุด ช่วงเวลา confidence เฉลี่ย
เฟรมทั้งหมด เฟรมที่ filter ตัด · **1 กล้อง 1 ปี = 365 แถว ไม่ถึง 100 KB**
ถูกจนไม่มีเหตุผลที่จะไม่ทำ และเป็นของที่ใช้ตอบผู้บริหารหรือเขียน paper ทีหลัง

### ภาพ

**ไม่เก็บ base64 ลง DB** 10 วิ/เฟรม = 8,640 ภาพ/วัน/กล้อง ราวใบละ 50 KB
เก็บทุกใบ = 3.5 GB/สัปดาห์/กล้อง

เก็บ **hash + metadata ทุกใบ** · เก็บ **ภาพจริงเฉพาะเฟรมที่เจอสัตว์ หรือเฟรมที่ filter ตัดทิ้ง**
(~10% = 350 MB/สัปดาห์) เฟรมที่โดนตัดต้องเก็บ **เพราะนั่นคือเฟรมที่จะอยากดูย้อนหลังที่สุด
ว่าตัดถูกหรือตัดพลาด** · บน App Platform ภาพก็หายตอน deploy เหมือนกัน POC ยอมรับได้

---

## LLM

| | dev | prod |
|---|---|---|
| provider | LM Studio `http://localhost:1234/v1` | OpenRouter |
| model | ตระกูล **Qwen** ตามที่ Toy กำหนด | qwen VL บน OpenRouter |

สลับด้วย env: `LLM_BASE_URL` `LLM_MODEL` `LLM_API_KEY` `LLM_PROVIDER`
ทั้งสองฝั่งเป็น OpenAI-compatible เลยใช้ `ChatOpenAI` ตัวเดียวกันได้

### วัดจริงแล้ว 2026-08-16 · qwen3.6-35b-a3b เห็นภาพได้

ผมเคยเขียนตรงนี้ว่ามันเป็น text-only **ซึ่งผิด** เดาจากชื่อรุ่นโดยไม่ทดสอบ
ทดสอบแบบเดาไม่ได้ (ก้อนสว่าง 7 ก้อน + รหัสสุ่ม `K7QZM`) ตอบถูกทั้งคู่ `blobs=7 code=K7QZM`

**บทเรียน: ยืนยันความสามารถด้วยภาพที่เดาไม่ได้ ไม่ใช่ด้วยชื่อรุ่น**
`tests/test_vision.py` ทำหน้าที่นี้ เรียกตอนสตาร์ตด้วย ถ้าอ่านรหัสไม่ออก = โมเดลไม่เห็นภาพ
ห้ามใช้ ต่อให้ชื่อจะมี VL ห้อยท้าย

### 🔴 ผลวัดที่กำหนดค่า max_tokens

| การตั้งค่า | เวลา/เฟรม | output | ผล |
|---|---|---|---|
| `max_tokens=200` | 10-11 วิ | 300 tok | ✅ ถูกเป๊ะ |
| `max_tokens=1500` | **37-39 วิ** | 1496 tok | ❌ คิดวนจนหลุดไปเขียนโค้ด cv2 ใส่ช่องคำตอบ |

**ยิ่งให้พื้นที่คิดเยอะ ยิ่งตอบแย่ลง** ไม่ใช่แค่ช้าลง · ความเร็วดิบ 40 tok/s ไม่ใช่ปัญหา
ปัญหาคือมันพ่น 1,500 token ทั้งที่คำตอบจริงยาว 20 token

เรื่องเดียวกับ [[projects/eval-harness/scaffold-agentic-finding]] · ปัญหาไม่ใช่โมเดลโง่
**ปัญหาคือมันไม่หยุด** โครงที่รัดแน่นเปลี่ยนโมเดลเดิมให้กลายเป็นคนละตัว

### แก้ตัวเลข 2026-08-16 หลังต่อท่อจริง

ค่า 200 ข้างบนวัดจาก prompt ทดสอบสั้นๆ **พอใช้ prompt จริงของระบบ มันไม่พอ**

| max_tokens | finish_reason | out | เวลา | ผล |
|---|---|---|---|---|
| 200 | `length` | 200 | 12.6 วิ | ❌ คิดไม่ทันจบ content ว่างเปล่า |
| 600 | `length` | 600 | 18.0 วิ | ❌ ตัดกลางประโยค |
| **1200** | **`stop`** | **621** | **17.7 วิ** | ✅ ตอบครบ `unknown` conf 0.9 พร้อมเหตุผล |

**หลักการเดิมยังถูก (รัดให้แน่น) แต่ตัวเลขต้องวัดต่อ model+prompt ห้ามลอกมา**
prompt ยาวขึ้น = ต้องการที่คิดมากขึ้น ค่าเริ่มต้นตอนนี้ **900** (เผื่อจาก 621 ที่วัดได้)

**17.7 วิ/เฟรม > รอบส่ง 10 วิ** อยู่ได้เพราะชั้น CV คัดออกเกิน 90% ก่อน
แต่ถ้าเฟรมมีสัตว์ติดกันหลายเฟรม จะตามไม่ทัน **นี่คือเหตุผลที่ควรลอง VL instruct
ที่ไม่ใช่สาย thinking** ไม่ใช่เรื่องความแม่นอย่างเดียว

**กติกาที่ตามมา**
- `max_tokens` รัดให้พอดีที่วัดได้ **ไม่ใช่กัน overflow แต่เพราะปล่อยยาวแล้วคำตอบแย่ลงจริง มีหลักฐาน**
- **บันทึก `finish_reason` ทุกครั้ง** ถ้า `length` = มันคิดจนหมดโควตา คำตอบเชื่อไม่ได้
  → ตี `degraded` ห้ามรับมาใช้เฉยๆ
- `enable_thinking: false` และ `/no_think` **ไม่มีผลกับโมเดลตัวนี้บน LM Studio** ลองแล้ว
  เวลาและ token เท่ากันเป๊ะ อย่าเสียเวลาลองซ้ำ

### 🔴 วัดจริง 2026-08-17 · qwen3-vl-30b-a3b-instruct บน OpenRouter ตอบ `{"animals":[]}` ทุกใบ

ยิงภาพตัวอย่างในรีโปทั้งสามใบผ่านท่อจริง (prompt v2, max_tokens 900, reasoning ปิด):

| model | three (3 ก้อน) | two (2 ก้อน) | one-far (1 ก้อน) | เวลา |
|---|---|---|---|---|
| qwen3-vl-30b-a3b-instruct | `[]` | `[]` | `[]` | 0.8-4.4 วิ |
| qwen3-vl-32b-instruct | unknown×3 conf 0.5 | unknown×2 conf 0.3 | `[]` | 0.5-0.9 วิ |
| qwen3.7-flash | unknown×3 conf 0.9 | unknown×2 conf 0.9 | unknown×1 conf 0.3 | 1.2-1.3 วิ |

**a3b ตอบว่าไม่มีสัตว์ทั้งที่ CV ส่ง bbox ไปให้แล้ว** นี่คือโหมดพังที่แย่ที่สุดของโปรเจคนี้
เงียบ ดูเหมือนเฟรมว่าง ปลายทางแยกไม่ออก ไม่ใช่แค่ตอบผิดชนิด

**ไม่ใช่ provider routing** ล็อกทีละเจ้าครบทั้งสี่ (Alibaba out=5 · DeepInfra out=5 ·
Novita out=5 · SiliconFlow out=4) ปิด fallback ทุกเจ้าตอบ `{"animals":[]}` เหมือนกันหมด
เป็นตัวโมเดลเอง **prod เลยลงเอยที่ `qwen3-vl-32b-instruct`** (ตั้งบน DO Console 2026-08-17)
ยิงเฟรมจริงเข้า prod แล้วได้ `unknown x3 conf 0.487 finish=stop` ทำงานได้

### 🔴 สมมติฐาน tiling ใช้ไม่ได้ที่ภาพ 640x512

prompt token ของภาพเดียวกัน: flash 751 · vl-32b 733 · vl-30b-a3b 733 **เท่ากันหมด**
VL ไม่ได้ให้ image token มากกว่าเลยที่ขนาดนี้ เพราะภาพเล็กกว่า tile เดียวอยู่แล้ว
**เหตุผลที่เขียนไว้ข้างล่างว่าย้ายมา VL แล้วช้างไกล 15x10 px จะรอด ใช้ไม่ได้**
ถ้าจะแก้เรื่องวัตถุเล็กจริง ต้องส่งภาพใหญ่ขึ้นหรือ crop รอบ bbox ส่งเข้าไป ไม่ใช่เปลี่ยนโมเดล

⚠️ ภาพสามใบนี้เป็นภาพสังเคราะห์ ไม่ใช่ภาพจากไซต์ **สรุปได้แค่ว่าบน OpenRouter a3b แย่กว่า
32b และ flash ที่ prompt ชุดนี้** สรุปไม่ได้ว่าจะแย่กับภาพจริง แต่ก็ไม่มีหลักฐานว่าจะดีเหมือนกัน
ที่ Toy ทดสอบแล้วแม่นคือ qwen3-vl บน LM Studio ซึ่งเป็นคนละ endpoint คนละ quantization

### เรื่องความละเอียด ข้อที่ยังไม่ทดสอบ

ภาพ 640×512 เข้าไปเป็น prompt **แค่ 430 token** = ถูกย่อลงเยอะก่อนเข้าโมเดล
ก้อนใหญ่ไม่มีปัญหา แต่ **ช้างที่ไกล 200 ม. กินพื้นที่ ~15×10 พิกเซล ย่อแล้วอาจหายไปเลย**

นี่คือเหตุผลเดียวที่มีน้ำหนักพอจะลอง Qwen3-VL: โมเดลสาย VL ทำ tiling ที่ความละเอียดเดิม
ได้ image token มากกว่าหลายเท่า + เทรนมาให้ตอบ bounding box ได้ ซึ่งเราต้องการเป็นรายก้อน

**ตัดสินด้วยตัวเลข ไม่ใช่ด้วยชื่อ** ยิงภาพชุดเดียวกันเข้าทั้งสองตัว วัด: ถูกกี่ครั้ง กี่วินาที ตรง schema ไหม

### LangChain ใช้แค่สองอย่าง

`ChatOpenAI` (สลับ base_url) + structured output ผูก Pydantic ให้ผลออกมาตาม schema แน่นอน
ไม่ต้อง parse ข้อความเอง

**ไม่ใช้ agent ไม่ใช้ memory ไม่ใช้ chain ซ้อน** งานนี้เป็น pipeline เส้นตรง
ยัด abstraction เกินจะ debug ยากเปล่าๆ

### ต้นทุน

8,640 เฟรม/วัน/กล้อง ยิง VLM ทุกเฟรมคือเงินจริงหลักพันบาท/เดือน/กล้อง
**CV เป็นด่านหน้า ไม่มีก้อนร้อนขนาดสัตว์ = ไม่ยิง LLM** ตอบ 0 ตัวไปตรงๆ
ซึ่งน่าจะเป็นกรณีส่วนใหญ่ ประหยัด >90% และเร็วกว่าด้วย

**timeout + fallback** เน็ตหลุด/LLM ช้า → ตอบ `degraded` พร้อมผล CV ไม่ค้างรอ

---

## ข้อจำกัดที่ต้องพูดตรงๆ ตั้งแต่วันแรก

**การระบุชนิดสัตว์จากภาพความร้อนล้วน มันยากกว่าที่ฟัง** ภาพ thermal ไม่มีสี ไม่มีลาย
เหลือแค่รูปทรงกับความต่างอุณหภูมิ ช้าง/ควาย/วัว ที่ระยะ 50 ม. ขึ้นไปคือก้อนขาวคล้ายกันหมด

ที่แยกได้จริงคือ **สัดส่วนรูปร่าง + ขนาดเชิงมุม** แต่ขนาดเชิงมุมแปลเป็นขนาดจริงไม่ได้ถ้าไม่รู้ระยะ
ต้องมีอย่างใดอย่างหนึ่ง: กล้องติดนิ่งแล้ว calibrate ระยะต่อพิกเซลของพื้นไว้ หรือมี RGB คู่กัน

ช้างพอมีลุ้นเพราะงวงกับหูเป็นเอกลักษณ์ในซิลูเอตต์ และตัวใหญ่สุดในฉาก
แต่ **confidence ที่ออกมาต้องสะท้อนความคลุมเครือนี้** ไม่ใช่ไปเจอตอนวัดผล
`unknown` ต้องมีอยู่ใน `counts` เสมอ ก้อนที่ระบุไม่ได้ไปอยู่ตรงนั้น
**ห้ามยัดเป็นช้างเพื่อให้ตัวเลขสวย**

### 🔴 รับเฉพาะภาพ thermal · ภาพชนิดอื่นต้องตอบว่าอ่านไม่เป็น ไม่ใช่ตอบว่าไม่มีสัตว์

เจอ 2026-08-16 · Toy ยิงภาพถ่าย RGB ช้างสองตัวเต็มเฟรมเข้ามา ระบบตอบ `200 OK` `counts: {}`
**หน้าตาเหมือนเฟรมที่ไม่มีอะไรจริงๆ ทุกประการ ปลายทางแยกไม่ออกเลย**

สมมติฐาน "ร้อน = สว่างกว่าพื้นหลัง" ฝังอยู่ทั้งท่อ (`segment()` หา `sm > thr` อย่างเดียว ·
`score()` นิยาม contrast เป็นสว่างกว่าพื้นหลัง · prompt v2 บอกโมเดลว่า "Bright regions are warm")
ในแสงกลางวันช้าง**มืดกว่า**ท้องฟ้า และ MAD ที่โตขึ้นหกเท่าดัน threshold ทะลุ 1.0 จน mask ว่าง

**เกณฑ์:** `MAX_THERMAL_MAD = 0.15` (`CV_MAX_THERMAL_MAD`) วัดมา ไม่ได้เดา
ภาพ thermal สังเคราะห์ทุกใบใน `tests/` อยู่ที่ 0.000-0.041 · ภาพถ่าย RGB จริง 0.234

**เตือนเฉพาะตอน CV ไม่เจอก้อนเลย** ถ้าเจอก้อน = ท่อทำงานได้กับภาพใบนี้จริง ห้ามเดาทับ
ประกาศ degraded ทั้งที่นับได้ = เปิดช่องให้เฟรมถูกต้องถูกโยนทิ้งเพราะสถิติดูแปลก
ซึ่งอันตรายกว่าปัญหาที่กำลังแก้ · กฎนี้เลยแยกสองอย่างที่เดิมหน้าตาเหมือนกันเป๊ะออกจากกัน

| ไม่เจอก้อนเพราะ | ตอบ |
|---|---|
| ไม่มีอะไรในเฟรม (90%+ ของกล้องจริง) | `ok` counts ว่าง เหมือนเดิม |
| อ่านภาพแบบนี้ไม่เป็น | `degraded` + `filtered.reason` มีเลข mad/threshold ที่ทำให้สงสัย |

**เคยตั้งเกณฑ์ที่สองไว้ว่า `threshold > 1.0` = อ่านไม่เป็น เหตุผลฟังขึ้นแต่ผิด**
เฟรม thermal จริงที่แดดส่องพื้นครึ่งล่างได้ `thr=1.037 mad=0.009` ซึ่งเป็นเฟรมปกติ
เทสต์ `test_normal_thermal_frames_are_never_flagged` ตีตก เลยตัดทิ้ง เหลือ MAD อย่างเดียว
**ธงที่ดังผิดแย่กว่าไม่มีธง เพราะปลายทางจะเรียนรู้ที่จะเมินมัน**

⚠️ `0.15` ต้องวัดใหม่กับภาพจากไซต์จริง (Toy ขอมาแล้ว 2026-08-16 รออยู่)
ฉากพลบค่ำที่ฟ้าเย็นจัดครึ่งเฟรม พื้นอุ่นครึ่งเฟรม ก็ทำให้ MAD โตได้เหมือนกัน ยังไม่มีตัวอย่างมาวัด

**ยังไม่รองรับ RGB และไม่ควรไปแก้ `thermal.py` ให้รองรับ** sign กลับด้าน logic คนละเรื่อง
ถ้าไซต์จะมีกล้อง RGB คู่จริง ต้องเขียน `app/cv/rgb.py` เป็น pipeline ที่สอง (ยังไม่ตัดสิน รอถามสุชาติ)

---

## ที่ยังไม่รู้ ต้องถาม Toy / สุชาติ

- กล้องส่ง **radiometric 16-bit** (มีค่าอุณหภูมิจริงทุกพิกเซล) หรือ **JPEG/PNG ผ่าน palette สีแล้ว**
  → เปลี่ยนโค้ด CV ทั้งท่อน และเปลี่ยนว่าใช้เกณฑ์อุณหภูมิได้หรือไม่ได้
  ระหว่างรอ: ตรวจ bit depth เองตอนรับภาพ รองรับทั้งสองแบบ
- **กล้องติดนิ่งไหม** ถ้านิ่งทำ background model ได้ แม่นขึ้นเยอะ
- **แยกกี่ชนิด** ช้าง/สัตว์อื่น/คน พอ หรือต้องแยกวัว ควาย กวาง หมูป่า ยิ่งเยอะยิ่งพัง
- auth/rate limit ของ endpoint
- จะกลายเป็น paper ด้วยไหม ถ้าใช่ ต้องวางชุดทดสอบกับ ground truth ตั้งแต่แรก

---

---

## LangSmith

เปิดใช้เพราะ **ทาง ค. ทำให้ SQLite หายทุก deploy** LangSmith เลยกลายเป็นที่เก็บถาวร
ที่อยู่นอก container เป็นที่เดียวที่ log การเรียก LLM รอดข้ามรอบ deploy

| ข้อมูล | เก็บที่ | รอด deploy |
|---|---|---|
| ผลวิเคราะห์ที่ปลายทางใช้ · หน้าต่าง 100 วิ | SQLite | ไม่ (ยอมรับแล้ว) |
| prompt · response ดิบ · latency · cost · error | **LangSmith** | **ใช่** |

### 🔴 กติกาสามข้อ ตั้งผิดแล้วเสียเงิน

Toy ออกทุนเอง งบจำกัด ค่าเริ่มต้นทุกตัวต้องเลือกทางที่ถูกที่สุดที่ยังไม่เสียของ

1. **sampling ไม่ใช่เก็บทุกอัน** 860 trace/วัน/กล้อง = 26,000/เดือน ทะลุแพ็กฟรี (~5,000/เดือน)
   ตั้งแต่เดือนแรก กล้องเดียว
   - **เก็บ 100%** เฉพาะเฟรมที่ผิดปกติ: `conf < 0.60` · filter ตัดทิ้ง · `degraded` ·
     `finish_reason == "length"` · `state == "unstable"`
   - **เก็บ 5%** ของเฟรมที่ปกติดี (`LANGSMITH_SAMPLE_RATE`)
   - ของที่จะกลับมาดูจริงคือเฟรมที่มีปัญหา ไม่ใช่เฟรมที่ถูกอยู่แล้ว
2. **ห้ามส่งภาพ base64 เข้า trace** payload บวมและกินโควตา storage เร็วกว่ากินโควตา trace
   ส่งแค่ `image_hash` + ขนาด + ผล CV ที่เป็นตัวเลข อยากเห็นภาพค่อยแนบ thumbnail 128px
   ผลพลอยได้: ภาพจากไซต์อนุรักษ์ไม่ต้องไปนอนบนเซิร์ฟเวอร์เจ้าอื่น POC ไม่คิดมากก็ได้
   แต่ถ้ามีสัญญากับหน่วยงานทีหลังจะเป็นประเด็น ตั้งถูกตั้งแต่แรกง่ายกว่ามาแก้
3. **ปิดได้ด้วย env** `LANGSMITH_TRACING=false` ตอน test/CI/dev
   **เทสต์ต้องรันได้ตอนไม่มีเน็ต** ไม่งั้นรัน test 50 ครั้งกิน quota 50 trace เปล่าๆ

## stack

Python · FastAPI · LangChain (`ChatOpenAI` + structured output) · SQLite (interface สลับได้) ·
numpy + scipy + Pillow สำหรับ CV · **ไม่มี torch ไม่มี GPU ใน container**

## git

repo `boonruang/animalcountllm` · branch `main`
**ห้าม push เองจนกว่า Toy สั่ง**

---
---

# งานที่สอง — smart people counting (`/people/*`)

เพิ่ม 2026-09-11 ตามที่ Toy สั่ง: **repo เดียวกัน แยก endpoint แยกฐานข้อมูล**
งานช้างไม่ถูกแตะเลย นอกจากสามบรรทัดท้าย `app/main.py` ที่ mount แอปตัวนี้เข้าไป

รับ **id + ภาพ** (ทีละคนหรือหลายคนในนัดเดียว) ตอบ **เพศ · ช่วงอายุ · รูปพรรณสัณฐาน**

โครงการต้นทาง: ระบบนับคนของทีม [[people/suchart-senanon]] ([[business/iplus-development]])
ที่หน้าประตูอาคาร ระบบเขา detect + track เองแล้ว เราเป็นชั้นที่ตอบว่า "คนคนนั้นหน้าตายังไง"

## endpoint

| method | path | ทำอะไร |
|---|---|---|
| POST | `/people/v1/persons` | ปลายทางชี้คนมาเอง · 1-16 คนต่อ request |
| POST | `/people/v1/frames` | เฟรมเดียว ไม่มีใครถูกชี้ โมเดลหาคนเอง |
| GET | `/people/v1/persons/{request_id}` | ดูผลย้อนหลังทั้ง request |
| POST | `/people/v1/persons/{request_id}/{object_id}/truth` | ground truth ที่คนตรวจแล้ว |
| POST | `/people/v1/maintenance/prune` | ลบของเกินอายุ (เรียกจาก cron ข้างนอก) |
| GET | `/people/healthz` | สถานะของงานนี้โดยเฉพาะ |
| GET | `/people/verify/` | หน้าเว็บทดสอบด้วยตา |

`/healthz` ของฝั่งช้างเพิ่มสองฟิลด์ `people_mounted` / `people_error` **เท่านั้น**
`/v1/frames` และรูป JSON ของมัน ไม่ขยับสักตัว (`tests/test_response_contract.py` เฝ้าไว้)

## request รับได้สองแบบ เพราะภาพตัวอย่างมาสองแบบ

```jsonc
// แบบ 1 · ปลายทางครอปมาแล้ว (เหมือน iplus_sample1 ที่มี ID:011/021/022 + crop ใบหน้า)
{"camera_id":"N3-1C23A","objects":[{"id":"021","image_base64":"..."}]}

// แบบ 2 · เฟรมเต็ม + กรอบ (เหมือน iplus_sample2 ที่เป็นเฟรม 1916x1080 มีสามกรอบ)
{"camera_id":"N3-1C23A","frame_base64":"...",
 "objects":[{"id":"011","bbox":[795,95,180,515]}]}
```

ส่งมาทั้งคู่ `image_base64` ชนะ · `bbox` เป็น `[x,y,w,h]` พิกเซลของเฟรมนั้น ไม่ใช่สัดส่วน

**ไม่บังคับว่าต้องมี `bbox` แต่บังคับว่าต้องมีอย่างใดอย่างหนึ่ง** object ที่มีแต่ `id`
โดนตีกลับพร้อมเหตุผล ไม่ใช่เดาว่าหมายถึงคนไหนในเฟรม (คำถามจาก Toy 2026-09-11)

response คืน **`image.bbox_used` = กรอบที่ครอปจริง `[x1,y1,x2,y2]`** หลังขยาย margin
และหนีบขอบภาพแล้ว ซึ่ง**ไม่ใช่ค่าเดียวกับที่ส่งมา** ปลายทางเอาไปยืนยันด้วยตาได้ว่า
"คนที่เราอ่าน" คือคนเดียวกับ "คนที่คุณชี้" ไม่ใช่คนที่ยืนติดกัน · `null` = ส่ง crop มาเอง

## response

```jsonc
{
  "request_id":"uuid","camera_id":"N3-1C23A","received_at":"...",
  "status":"ok",                       // ok | partial | degraded | error
  "persons":[{
    "id":"021","status":"ok",
    "gender":"female","gender_confidence":0.9,
    "age_range":"20-29","age_range_confidence":0.5,
    "appearance":{
      "skin_tone":"medium","build":"slim","height":"average",
      "hair_length":"tied_back","hair_color":"black",
      "glasses":"no","face_mask":"no","facial_hair":"no","headwear":"no",
      "top":{"type":"shirt","color":"white","secondary_color":"unknown","pattern":"plain"},
      "top_sleeve":"long",
      "bottom":{"type":"trousers","color":"black",...},
      "footwear":{"type":"leather_shoes","color":"black",...},
      "carrying":["lanyard"],
      "distinctive":"สายคล้องคอสีเขียว"},
    "appearance_confidence":0.7,
    "description":"หญิงไทย อายุราว 25 ปี ผมรวบ ใส่เสื้อเชิ้ตขาว ...",
    "overall_confidence":0.7,"reason":"",
    "image":{"w":224,"h":515,"source":"frame_bbox","bbox_used":[780,80,990,625]},
    "model":{...},"timing_ms":8500.0}],
  "summary":{"objects_in":3,"ok":3,"degraded":0,"error":0},
  "model":{...},"timing_ms":{"decode":140,"vlm":9100,"vlm_sum":25300,"total":9250}
}
```

**`status` ระดับ request มีสามค่าโดยตั้งใจ** ส่งมา 5 คน อ่านได้ 4 ไม่ใช่ทั้งสำเร็จ
และไม่ใช่ทั้งล้มเหลว · `partial` บอกปลายทางว่าให้ retry เฉพาะคนที่ไม่ได้

**`vlm` คือเวลาจริงที่รอ (ยิงขนาน) · `vlm_sum` คือเวลารวมที่จ่ายเงิน** สองตัวนี้ไม่เท่ากัน
และตัวที่ใช้คิดต้นทุนคือตัวหลัง

## กฎที่ยืมมาจากฝั่งช้างทั้งดุ้น อย่าเรียนซ้ำด้วยเงินตัวเอง

1. **`unknown` ≠ `degraded`**
   `unknown` = ดูแล้วแต่ดูไม่ออก (จบแล้ว) · `degraded` = ยังไม่ได้ดู โมเดลล่ม (retry ได้)
   ฝั่งช้างเคยให้ `provisional` ทับ `degraded` แล้วปลายทางอ่านว่า "รอแป๊บ"
   ทั้งที่ความจริงคือโมเดลใช้ไม่ได้อยู่ตอนนั้น
2. **ห้ามเดาเพื่อให้ช่องไม่ว่าง** ทุกฟิลด์เป็น `unknown` ได้ และค่าเริ่มต้นคือ `unknown`
   `people/llm/client.py:normalize()` มีหน้าที่ **ลดทอนอย่างเดียว ห้ามเติม**
   "เสื้อขาว" ที่เดาผิดส่งคนไปตามผิดคน แพงกว่าช่องว่างมาก
3. **ค่านอกชุดกลายเป็น unknown ไม่ใช่ 500** โมเดลตอบ `"light-medium"` หรือ `"ชาย"` ได้ทั้งนั้น
   ปล่อยหลุดถึง pydantic = คนคนนั้นหายไปทั้งคน ทั้งที่ฟิลด์อื่นอ่านได้ครบ
   **เสียหนึ่งช่อง ไม่ใช่เสียทั้งคน**
4. **หนึ่งคนต่อหนึ่งครั้งที่ยิงโมเดล** ไม่ยัดหลายคนใน prompt เดียวแม้จะถูกกว่า
   เพราะคำบรรยายจะไหลข้ามคน (คนที่ 2 ได้เสื้อของคนที่ 1) ซึ่งปลายทางตรวจไม่เจอ
   หน้าตาเหมือนคำตอบปกติทุกประการ
5. **เพดาน 16 คนต่อ request** ไม่มีเพดาน = ปลายทางส่ง 200 คนแล้วเราจ่ายค่าโมเดล 200 ครั้ง
   ในนัดเดียวโดยไม่มีใครเห็นจนบิลมา
6. **ห้ามบอกข้อสรุปให้โมเดลก่อน** บทเรียน prompt v2 ของฝั่งช้าง ที่เขียนว่า
   "detector found 0 warm regions" แล้วโมเดลเชื่อ ตอบว่าไม่มีสัตว์ทั้งที่ช้างเต็มเฟรม
7. **ขอเฉพาะสิ่งที่โมเดลทำได้จริง** ไม่ขอ bbox (ฝั่งช้างได้ y=998 บนภาพสูง 628)
   ไม่ขอส่วนสูงเป็นเซนติเมตร ไม่ขออายุเป็นตัวเลขปี · ช่วงอายุกับค่าเทียบเคียงเท่านั้น

## ขอบเขตที่ตั้งใจไม่ทำ

**บรรยายรูปพรรณ ไม่ระบุตัวบุคคล** ไม่จับคู่ใบหน้า ไม่มี watchlist ไม่มีทะเบียนคน
ไม่เดาชื่อ สัญชาติ เชื้อชาติ ศาสนา อาชีพ · `skin_tone` คือสีผิวที่ปรากฏในภาพใบนั้น
ภายใต้แสงนั้น เป็นค่าสำหรับใช้ตามตัวเหมือนสีเสื้อ และต้องอ่านคู่กับ confidence เสมอ

เหมือนฝั่งช้าง: **ไม่ยิงใคร ไม่เตือนใคร ไม่ตัดสินใจแทนใคร** ห้ามเพิ่ม outbound webhook
หรือคิวที่ยิงกลับไปหาปลายทางลงในแพ็กเกจนี้

## ข้อมูล

**ฐานข้อมูลแยกไฟล์จากฝั่งช้าง** (`PEOPLE_STORE_DSN`) คนละตาราง คนละ connection
คนละ retention · `persons` / `person_requests` / `person_truth`
บน App Platform หายทุก deploy เหมือนกัน ทาง ค. เดิม

**ค่าเริ่มต้นไม่เก็บภาพ** (`PEOPLE_SAVE_IMAGES=none`) ในภาพคือหน้าคนที่เดินเข้าอาคาร
ไม่ใช่สัตว์ในป่า · `degraded` = เก็บเฉพาะคนที่อ่านไม่ออก (ชุดที่มีค่าตอนดีบัก)
`all` = ไล่บั๊กเท่านั้น อย่าเปิดค้างบน prod
**ภาพตัวอย่างจากทีมคุณสุชาติห้าม commit ลง repo** repo เป็น public และในภาพมีหน้าคนจริง
เทสต์ใช้ภาพสังเคราะห์ที่สร้างเองใน `tests/test_people_contract.py:_png()`

## ต้นทุน

ยังไม่ได้วัดกับ OpenRouter เพราะ `.env` ในเครื่อง dev ไม่มีคีย์จริง (คีย์อยู่ใน DO Console)
ประมาณจากรูปคำตอบ: prompt ~950 tok + completion ~300 tok ต่อคน
ที่ `qwen3-vl-32b-instruct` (0.104/0.416 ต่อ 1M) = **~$0.00022 ต่อคน**
1,000 คนที่เดินผ่านประตูต่อวัน ≈ **$6.6/เดือน/กล้อง** · 5,000 คน/วัน ≈ $33

🔴 **ตัวแปรที่คุมต้นทุนคือ "ยิงกี่คน" ไม่ใช่ "โมเดลตัวไหน"** ปลายทางต้องเป็นคนตัดสินว่า
ยิงทุก track หรือยิงครั้งเดียวต่อคนต่อการเข้าออกหนึ่งครั้ง · ถ้ายิงทุกเฟรมของ track เดียวกัน
ตัวเลขข้างบนคูณด้วยจำนวนเฟรมทันที

## ยังไม่ได้วัด ต้องวัดก่อนเชื่อ

- **ยังไม่เคยยิง OpenRouter จริงสักครั้ง** (ไม่มีคีย์ในเครื่อง dev)
  ลองกับ LM Studio แล้ว `qwen3.6-35b-a3b` **ใช้ไม่ได้กับงานนี้**: 488 วิ สำหรับ 3 คน
  แล้วได้ `empty response` 2 คน timeout 1 คน · เป็นโมเดลสายคิดยาว คำตอบ 25 ฟิลด์
  ทำให้มันคิดไม่จบ (เรื่องเดียวกับที่ฝั่งช้างเจอ)
- `PEOPLE_MIN_CROP_PX=224` ช่วยเรื่องความแม่นจริงไหม **ยังไม่รู้** ตั้งไว้กัน provider
  ปฏิเสธภาพจิ๋ว · ฝั่งช้างวัดแล้วว่าที่ 640x512 prompt token ไม่ขยับตามขนาดภาพ
  อย่าเชื่อว่ายิ่งใหญ่ยิ่งแม่นจนกว่าจะวัดกับ crop จริง
- `PEOPLE_LLM_MAX_TOKENS=700` เผื่อไว้ ยังไม่ได้วัดว่าคำตอบจริงยาวเท่าไร
  ดู `finish_reason` ใน DB · ถ้าเจอ `length` แปลว่าน้อยไป ถ้าจบที่ 300 ตลอดก็ลดได้
- ปลายทางจะส่ง crop มาเอง หรือส่งเฟรมเต็ม+bbox **ยังไม่ได้ถามทีมคุณสุชาติ** รองรับไว้ทั้งคู่แล้ว
- ยังไม่ได้ confirm รูป JSON กับปลายทาง · `tests/test_people_contract.py` ล็อกรูปไว้แล้ว
  วันที่ confirm เมื่อไหร่ ไฟล์นั้นกลายเป็นด่านจริง ห้ามแก้อีกนอกจากปลายทางตกลงด้วย

## รันตอน dev

```
uvicorn app.main:app --reload --port 8080
# แล้วเปิด http://127.0.0.1:8080/people/verify/
```
หน้าเว็บอยู่ origin เดียวกับ API อยู่แล้ว **ไม่ต้องใช้ `ui/serve.py`** และเปิดจาก `file://`
ยังใช้ไม่ได้เหมือนเดิม

## เส้นที่สอง `/people/v1/frames` — ทั้งเฟรม ไม่มีใครถูกชี้

เพิ่ม 2026-09-11 ตามที่ Toy สั่ง · **body เหมือน spec ฝั่งช้างเป๊ะ หนึ่ง request หนึ่งภาพ**

```json
{"camera_id":"cam-01","image_base64":"/9j/4AAQ...","ts":"2026-08-16T06:30:00+07:00",
 "note":"optional free text"}
```

`tests/test_people_contract.py:test_body_ของ_frames_เหมือน_spec_ช้างเป๊ะ` เทียบฟิลด์กับ
`app.schemas.FrameIn` **ของจริง** ไม่ใช่กับรายการที่พิมพ์ไว้เอง ฝั่งโน้นขยับเมื่อไหร่เทสต์ตกทันที

response รูปเดียวกับ `/v1/persons` ทุกประการ ต่างที่:
- `persons[].id` เป็น `P1 P2 P3` ที่**มีความหมายเฉพาะในคำตอบนี้** ผูกกับ track id ไม่ได้
- `persons[].where` เป็นข้อความบอกตำแหน่ง ("centre foreground, in front of the other two")
  **ไม่ใช่ bbox และจะไม่มีวันเป็น bbox** โมเดลคืนพิกัดมั่ว วัดมาแล้วฝั่งช้าง (y=998 บนภาพสูง 628)
- `summary.people_found` คือจำนวนคนที่เจอ · `objects_in` เป็น 0 เพราะไม่มีใครถูกส่งมาให้ตรวจ
- `reason` ระดับ request บอกว่าทำไมถึง degraded เพราะไม่มีคนสักคนให้ใส่เหตุผลไว้ข้างใน

🔴 **`{"people":[]}` = ล็อบบี้ว่าง ซึ่งเป็นคำตอบจริง ไม่ใช่ความล้มเหลว**
คำตอบที่ไม่มีคีย์ `people` เลย = โมเดลไม่ทำตามที่สั่ง → `degraded`
สองอย่างนี้ห้ามยุบเป็นอันเดียว ไม่งั้นเฟรมที่โมเดลพังจะหน้าตาเหมือนล็อบบี้ว่างเป๊ะ
ซึ่งคือบั๊กเดียวกับที่ฝั่งช้างเสียเวลาไปทั้งวัน

ชุดค่าที่ยอมรับ **ไม่ได้ก๊อปไปไว้สองที่** `prompt_f1` import `prompt_p1.ALLOWED` ก้อนเดียวกัน
(บทเรียนสดจากบั๊ก `laptop` เมื่อเช้าวันเดียวกัน)

### 🔴 วัดจริง 2026-09-11 · ตัวเลขนี้กลับข้างกับที่เดาไว้ตอนแรก

ตอนลองครั้งแรกด้วย prompt สั้นๆ ที่ไม่มีชุดค่า ได้ 7.1 วิ / 567 tok แล้วสรุปว่า
"เร็วกว่า 3 เท่า ถูกกว่า 1.4 เท่า" · **พอใส่ชุดฟิลด์จริงครบ ข้อสรุปนั้นหายไปหมด**

| | `/v1/persons` (3 คน ขนาน) | `/v1/frames` (3 คน) |
|---|---|---|
| prompt token | 4,020 (1,340 × 3) | **3,431** |
| completion token | 879 (293 × 3) | 962 |
| ราคา | $0.000784 | $0.000757 |
| wall clock | ~20 วิ | 16-27 วิ |
| ผูก track id ของปลายทาง | **ได้** | ไม่ได้ |
| ต้องมี detector มาก่อน | ต้องมี | **ไม่ต้อง** |

**ราคาเท่ากันในทางปฏิบัติ เวลาก็พอกัน** ที่ต่างกันจริงมีข้อเดียว: มี id ผูกกลับหรือไม่มี
ใครมี bbox อยู่แล้วให้ใช้ `/v1/persons` **ไม่มีเหตุผลที่จะทิ้ง id ไปแลกอะไรเลย**
`/v1/frames` มีค่าตอนไม่มีใคร detect มาให้ เช่นไล่ดูภาพย้อนหลัง หรือให้ทีมปลายทางลองเล่น

⚠️ **latency แกว่งตาม provider ที่ OpenRouter สุ่มให้** ยิงเดี่ยววัดได้ 5.2 วิ
ยิงสามเส้นขนานพร้อมกันกลายเป็น ~20 วิ/คน · ยิงเดี่ยวบน prod ได้ 12.5 วิ
เลขพวกนี้จึงเป็นช่วง ไม่ใช่ค่าคงที่ อย่าเอาไปสัญญากับปลายทางเป็นตัวเดียว

### ที่ยังไม่รู้

- `iplus_sample1.jpg` โมเดลรายงาน 2 คน (สองคนหน้ากล้อง) ทั้งที่ในเฟรมมีคนนอกกระจก
  กับคนที่ประตูด้วย · **ยังไม่ได้ตัดสินว่านั่นถูกหรือผิด** ขึ้นกับว่าปลายทางนับใครเป็น "คนในเฟรม"
  ต้องถามทีมคุณสุชาติ ไม่ใช่เดาเอง
- เส้นนี้กับเส้น persons ตอบไม่ตรงกันในรายละเอียดบางช่อง (กางเกง/กระโปรง · สูท/แจ็คเก็ต)
  ยังไม่มี ground truth เลยยังไม่รู้ว่าใครถูก `/truth` มีไว้เก็บตรงนี้

### ยิงผิดเส้นต้องบอกว่าผิดเส้น (2026-09-11)

Toy ส่ง body แบบเฟรมเดียวมาที่ `/v1/persons` แล้วได้ `objects: Field required`
ซึ่งถูกต้องแต่ช่วยอะไรไม่ได้ · ตอนนี้ 422 มี `hint` ต่อท้ายบอกว่าให้ไปเส้นไหน
`detail` ยังเป็นรูปเดิมของ FastAPI เป๊ะ ปลายทางที่ parse อยู่แล้วไม่พัง

🔴 **ไม่แก้ด้วยการให้ `/v1/persons` รับ body แบบเฟรมเดียวแล้วเดาว่าเขาหมายถึงอะไร**
endpoint ที่แอบทำงานคนละอย่างกับชื่อตัวเอง คือบั๊กที่หาไม่เจอทีหลัง

และ `image.source` ของเส้น `/v1/frames` เคยคืน `object_image` ซึ่งแปลว่า
"ปลายทางครอปมาให้หนึ่งคนแล้ว" **ซึ่งไม่จริง มันคือเฟรมเต็ม** ตอนเขียนมีให้เลือกแค่
สองค่าเลยหยิบอันที่ใกล้ที่สุด · ฐานข้อมูลเขียนว่า `"frame"` อีกค่าหนึ่ง ไม่ตรงกันอีก
ตอนนี้เป็น `full_frame` ทั้งสองที่ · **ค่าที่ไม่มีให้เลือก ห้ามหยิบค่าใกล้เคียงมาใส่แทน
ให้เพิ่มค่าใหม่ ไม่งั้นทั้งฟิลด์จะเชื่อไม่ได้**

## `direction` เข้า/ออก + ซ่อน `model` (v0.3.0 · 2026-09-11)

Toy สั่งสองอย่าง: ซ่อนข้อมูลโมเดลจากปลายทาง และเพิ่มทิศเข้า/ออก

### `direction`: `in` · `out` · `unknown`

ชื่อ key เลือก `direction` ไม่ใช่ `walk` เพราะ `walk` อ่านแล้วเหมือนถามว่า
"เดินอยู่ไหม" ซึ่งเป็น boolean · อยู่ระดับบนของคนแต่ละคน คู่กับ `direction_confidence`

```jsonc
"persons":[{"id":"P1","direction":"in","direction_confidence":0.9, ...}],
"summary":{"direction_in":2,"direction_out":1,"direction_unknown":0, ...}
```

🔴 **`direction_unknown` อยู่ใน summary เสมอ ห้ามถอด** in + out + unknown ต้องเท่า
จำนวนคนเสมอ · ปลายทางที่เห็นแต่ in กับ out จะบวกเป็นยอดรวมแล้วไม่รู้ว่าขาดไปกี่คน
**ตัวเลขที่ไม่ครบแต่ดูครบ แย่กว่าตัวเลขที่บอกว่าตัวเองไม่ครบ**

⚠️ **ค่านี้มาจาก "หันหน้าไปทางไหนในภาพนิ่งใบนี้" ไม่ใช่ "กำลังเดินไปทางไหน"**
คนยืนคุยโทรศัพท์หันหน้าเข้ากล้องได้โดยไม่ได้เข้าไปไหน คนเดินออกแล้วหันกลับมามอง
ข้างหลังก็ได้ · ภาพนิ่งใบเดียวแยกสองอย่างนี้ไม่ได้ และเราไม่แกล้งทำเป็นแยกได้

**ถ้าปลายทางต้องการยอด "เข้ากี่คน ออกกี่คน" ที่เชื่อถือได้จริง ต้องนับจาก track
ที่ข้ามเส้นในวิดีโอ ซึ่งระบบของทีมคุณสุชาติทำอยู่แล้ว** (เห็น "เข้า 11 ออก 5"
บนหน้าจอเขาใน `iplus_sample1`) ของเราใช้ประกอบ ใช้แทนกันไม่ได้

**วัดจริง 2026-09-11 · qwen3-vl-32b-instruct**

| ภาพ | ผล |
|---|---|
| `iplus_sample2` (3 คนเดินเข้า) | in 3 · out 0 · unknown 0 ✅ |
| `iplus_sample1` | in 2 · out 1 · unknown 0 ✅ คนที่สามอยู่นอกประตูกระจก เดินไปหารถตู้ ตอบ `out` conf 0.6 |

⚠️ **ผลข้างเคียงที่ต้องรู้: พอเพิ่มคำถามเรื่องทิศ โมเดลเจอคนใน `iplus_sample1`
เป็น 3 คน จากเดิม 2 คน** คนที่เพิ่มมาคือคนนอกกระจกที่เมื่อก่อนมันข้าม
เปลี่ยน prompt แล้วจำนวนคนที่เจอขยับได้ **นี่คือเหตุผลที่ห้ามสรุปความแม่นจากภาพใบเดียว
และห้ามเปลี่ยน prompt แล้วไม่ยิงภาพชุดเดิมซ้ำ**

### `model` ถูกซ่อน

`PEOPLE_EXPOSE_MODEL` ค่าเริ่มต้น `false` · `model` ในคำตอบเป็น `null` ทั้งระดับบน
และรายคน · `/people/healthz` ก็ซ่อน `provider` `model` `prompt_version` ด้วย
(หน้านั้นเปิดได้โดยไม่ต้องมีคีย์ ซ่อนที่เดียวไม่พอ)

🔴 **คีย์ยังอยู่ ไม่ได้ถอดทิ้ง** รูป response ที่เปลี่ยนรูปตามค่า env คือของที่ทำให้
ปลายทาง parse พังแบบหาสาเหตุไม่เจอ · ของจริงยังเก็บครบใน DB ไล่ย้อนหลังได้เหมือนเดิม
ตอน dev เปิด `PEOPLE_EXPOSE_MODEL=true` เพื่อดู `finish_reason` กับจำนวน token

### ฐานข้อมูลเก่าต้อง migrate

`CREATE TABLE IF NOT EXISTS` ไม่แตะตารางที่มีอยู่แล้ว ตารางเก่าจึงไม่มีคอลัมน์
`direction` แล้ว INSERT พังทุกแถว · `PersonStore._migrate()` เติมให้เองด้วย
`ALTER TABLE ADD COLUMN` · **บั๊กแบบนี้ prod ไม่มีวันเจอเพราะไฟล์หายทุก deploy
แต่เครื่อง dev เจอตลอด ซึ่งแปลว่ามันจะถูกมองข้าม**

## `client_request_id` — ปลายทางอ้างอิงคำตอบของตัวเอง (v0.4.0 · 2026-09-11)

คำถามจาก Toy: *"ผู้ใช้จะทราบได้อย่างไรว่า response ที่ตอบกลับมาเป็นของตนเอง"*

ตอบสองชั้น **HTTP คู่กันอยู่แล้ว** response ที่กลับมาคือของ request ที่ยิงไป
ไม่มีทางสลับกับของคนอื่น เว้นแต่ฝั่งเขาเอาไปเข้าคิวแล้วจับคู่ทีหลังเอง

**แต่** `request_id` ที่เราออกให้ ปลายทางจะรู้ก็ต่อเมื่อได้ response แล้ว
🔴 **ถ้า timeout หรือเน็ตหลุดกลางทาง เขาจับคู่อะไรไม่ได้เลย** ซึ่งเป็นตอนเดียว
ที่เขาอยากรู้ผลที่สุด · `client_request_id` แก้ตรงนี้ เพราะเขารู้ค่ามันตั้งแต่ก่อนยิง

**ทีมคุณสุชาติจะส่งเลขอ้างอิงมาในช่อง `note`** ซึ่งมีอยู่แล้วใน spec ฝั่งช้าง
เขาเลยไม่ต้องแก้ body ที่ส่งอยู่เลย (Toy เคาะ 2026-09-11) · คืนกลับที่ `ref`

```jsonc
// ส่งไป · ทางที่ปลายทางจะใช้จริง
{"camera_id":"cam-01","image_base64":"...","note":"iplus-20260911-000123"}

// ตอบกลับ
{"request_id":"<uuid ของเรา>","ref":"iplus-20260911-000123", ...}
```

`ref` = `client_request_id` ถ้าส่งมา ไม่งั้นเอา `note` · ใครอยากใช้ช่องที่ชื่อตรงกว่า
ส่ง `client_request_id` มาได้ ตัวนั้นชนะ

🔴 **มี `ref` ช่องเดียว ไม่คืน `note` ซ้ำอีกช่อง** ค่าเดียวกันสองชื่อคือที่ที่คนอ่าน
เอกสารจะเดาผิดว่ามันต่างกันตรงไหน · และ **คืนตัวเดิมเป๊ะ ไม่ trim ไม่แปลง**
ของที่ปลายทางเอาไปเทียบว่า "ใช่ของฉันไหม" ห้ามถูกเราแตะระหว่างทาง

`GET /people/v1/persons/by-ref/{ref}` ตามผลด้วยเลขของเขาเอง (หาทั้งจาก `note`
และ `client_request_id`)
คืน `matches` (เจอกี่อัน) + `latest` · **เราไม่การันตีว่าไม่ซ้ำ เพราะเลขนี้ไม่ใช่ของเรา**
ส่งซ้ำมาก็มีหลายผล บอกไปตรงๆ ว่ากี่อัน ไม่แกล้งเลือกให้อันเดียวเงียบๆ

⚠️ **ไม่มี idempotency** ส่ง `client_request_id` เดิมซ้ำ = ยิงโมเดลใหม่ = จ่ายใหม่
ไม่ได้คืนของเดิมจากแคช · ถ้าปลายทางจะ retry อัตโนมัติ ต้องรู้ข้อนี้ก่อน
(ทำได้ถ้าต้องการ แต่ต้องตัดสินใจว่า "เดิม" หมายถึงภายในกี่นาที ยังไม่มีใครสั่ง)

🔴 `client_request_id` เป็น **optional เท่านั้น** ห้ามทำให้บังคับ ไม่งั้น body เดิม
ของฝั่งช้างจะยิงไม่ผ่าน ซึ่งผิดคำสัญญาว่า "เปลี่ยนแค่ path"
`test_body_ของ_frames_รับของฝั่งช้างได้ครบทุกฟิลด์` เฝ้าไว้ทั้งสองข้อ:
ฟิลด์ของฝั่งช้างต้องรับได้ครบ **และ** ฟิลด์ที่เราเพิ่มต้องไม่บังคับ

### ship.py เคยไม่เห็นเทสต์ฝั่งคนเลย (แก้แล้ว 2026-09-11)

`check_tests()` รันแค่ `test_cv.py` กับ `test_filters.py` ซึ่งเป็นสองไฟล์ที่มีอยู่
ณ วันที่เขียน ship.py · เทสต์ฝั่งคนอีก 60 กว่าข้อ ด่านนี้ไม่เคยเห็น
**วันนี้ commit ที่มีเทสต์ตกขึ้น main ไปเรียบร้อยโดยด่านไม่ร้องสักคำ**
ตอนนี้รัน `pytest tests` ทั้งโฟลเดอร์ · **ด่านที่ตรวจจากรายชื่อไฟล์ จะล้าสมัยเงียบๆ เสมอ**

และเทสต์ที่ตกนั้นตกเพราะ **ฐานข้อมูลเทสต์อยู่ข้ามรอบ** ใช้ `client_request_id`
เป็นค่าคงที่ รอบแรกเขียว รอบสองเจอของเก่าค้างแล้วนับได้ 6 แทนที่จะเป็น 2
เทสต์ที่เขียวรอบแรกแล้วตกรอบสอง แย่กว่าตกตั้งแต่แรก · ตอนนี้ ref ในเทสต์เป็น uuid

## Demographic + uniform + อารมณ์ (v0.6.0 · 2026-09-12)

Toy สั่งสามอย่างในรอบเดียว: **Demographic Analytics · uniform detection · สัญชาติ**
แล้วสั่งเพิ่มระหว่างทางว่า **อารมณ์เป็นสเกล 1-5 (1 = bad, 5 = very happy)**

### ของใหม่ในคำตอบ

| ฟิลด์ | อยู่ที่ | มาจากไหน |
|---|---|---|
| `age_group` | ระดับคน | **คำนวณจาก `age_range`** ไม่ได้ถามโมเดล |
| `emotion.label` / `.valence` / `.confidence` | ระดับคน | ถามโมเดล · valence 1-5, `0` = มองไม่เห็นหน้า |
| `group.ref` / `.size` / `.type` | ระดับคน | ถาม **ป้ายกลุ่ม** จากโมเดล แล้ว**เรานับขนาดเอง** |
| `nationality` / `_confidence` | ระดับคน | 🔴 **ปิดอยู่** `unknown` เสมอ ดูหัวข้อล่าง |
| `appearance.uniform.{kind,color,id_badge,text}` | ใน appearance | ถามโมเดล |

`summary` เพิ่ม 19 ตัวนับ: `age_*` · `gender_*` · `in_uniform` + `uniform_unknown` ·
`group_*` + `groups_found` · `emotion_positive/neutral/negative/unknown`

🔴 **ทุกชุดต้องบวกได้ครบจำนวนคน และทุกชุดต้องมีถัง unknown** กฎเดียวกับ `direction`
เป๊ะ · `test_ยอดรวม_demographic_ต้องบวกได้ครบจำนวนคนทุกชุด` เฝ้าไว้ทั้งสองเส้น

**ไม่มีค่าเฉลี่ยอารมณ์ใน summary โดยตั้งใจ** `summary` เป็น `Dict[str, int]` และ
ค่าเฉลี่ยที่นับคนที่ valence=0 เข้าไปด้วย จะดิ่งลงตามจำนวนคนที่มองไม่เห็นหน้า
ซึ่งอ่านเหมือน "ลูกค้าอารมณ์แย่ลง" ทั้งที่แปลว่า "กล้องเห็นหน้าน้อยลง"
ใครอยากได้ ให้เฉลี่ยจาก `persons[]` เฉพาะคนที่ `valence > 0`

### อารมณ์: Ekman 7 + สเกล 1-5 สองชั้นในก้อนเดียว

Toy เสนอชุด Ekman 7 มาเอง (2026-09-12) ซึ่งตรงกับที่ทำไว้แล้ว **แต่เปลี่ยนสะกด**

| `label` | ไทย | ลักษณะใบหน้าที่บอกโมเดลไว้ใน prompt | `valence` |
|---|---|---|---|
| `happy` | มีความสุข | ยิ้ม มุมปากยก | 4-5 |
| `neutral` | เป็นกลาง | ไม่มี expression เด่นชัด | 3 |
| `surprise` | ประหลาดใจ | ตาโต ปากอ้า คิ้วยกโค้ง | 2-4 |
| `sad` | เศร้า | มุมปากตก คิ้วด้านในยก | 1-2 |
| `fear` | กลัว | ตาเบิกกว้าง คิ้วยกและบีบเข้าหากัน | 1-2 |
| `disgust` | รังเกียจ | จมูกย่น ริมฝีปากบนยก | 1-2 |
| `angry` | โกรธ | คิ้วขมวดลง หน้าตึง | 1-2 |
| `unknown` | มองไม่เห็นหน้า | หันหลัง แมสก์ ไกล มืด | **0** |

🔴 **สะกดตาม FER2013/AffectNet** (`fear` `surprise` `disgust` ไม่ใช่ `fearful`
`surprised` `disgusted`) เพราะจอของทีมคุณสุชาติโชว์คำว่า "Neutral" อยู่แล้ว
แปลว่าเขามี FER ของเขาเอง **สะกดเหมือนกัน = เอาสองค่ามาเทียบกันได้ตรงๆ**
ไม่ต้องมีตารางแปลงที่จะล้าสมัยเงียบๆ วันที่ฝ่ายใดฝ่ายหนึ่งเพิ่มค่า
แต่รับสะกดรูป adjective ไว้ใน `_EMOTION_ALIAS` ด้วย เพราะโมเดลชอบตอบ "surprised"
**ทิ้งคำตอบที่ถูกแล้วเพราะสะกดคนละแบบ คือบั๊ก `laptop` เป๊ะๆ**

**คอลัมน์ "ลักษณะใบหน้า" อยู่ใน prompt จริง ไม่ได้อยู่แค่ในตารางนี้** มันบังคับให้
โมเดลดูที่คิ้วที่มุมปาก แทนที่จะเดาจากบรรยากาศรวมของภาพ ซึ่งเป็นหลักเดียวกับทั้ง prompt
(ถามเฉพาะสิ่งที่มองเห็นได้)

⚠️ **`fear` กับ `disgust` ที่หน้าประตูอาคารแทบไม่ควรขึ้นเลย** สองตัวนี้ FER จริง
พลาดบ่อยที่สุดและถูกกุขึ้นมาง่ายที่สุด ไม่ถอดออกจากชุด (ถอดแล้วโมเดลจะประดิษฐ์คำอื่น
มาแทน) แต่ยอดสองตัวนี้ที่ไม่ใช่ศูนย์ = สัญญาณให้ไปเปิดภาพดู ไม่ใช่ข้อค้นพบ

⚠️ **หน้านิ่งปกติของคนไทยหลายคนอ่านเป็น angry/sad ในโมเดลที่เทรนจากฝรั่ง**
อคตินี้มีจริงและ **เราไม่มี ground truth สักแถวที่จะบอกว่าหนักแค่ไหน** ต้องเก็บ `/truth`

### ค่าเป็นไทย คีย์เป็นอังกฤษ (`people/th.py`)

Toy สั่ง 2026-09-12 · `"gender": "ชาย"` · `"glasses": "ใส่"` · `"direction": "เข้า"`

🔴 **แปลตอนขาออกที่เดียว ไม่ได้สั่งโมเดลตอบไทย**

สั่งโมเดลว่า "ตอบสีเป็นภาษาไทย" แล้วมันจะตอบ "ขาวนวล" "ครีมอมเทา" "ดำด้าน"
ถูกทุกคำในภาษาไทย แต่ **กรองด้วยโปรแกรมไม่ได้** และ `_pick` จะโยนทิ้งเป็น unknown หมด
ชุดค่าปิดมีไว้ให้ปลายทางกรองได้ ("หาคนเสื้อแดง") ภาษาไทยที่โมเดลแต่งเองทำลายมันทันที

เลยแยกสองชั้นคนละหน้าที่:

| ชั้น | ภาษา | ทำไม |
|---|---|---|
| prompt · `normalize` · ฐานข้อมูล | **อังกฤษ ชุดค่าปิด** | ของที่ใช้ทำงาน กรอง เทียบ GROUP BY |
| `_out()` ตอนตอบกลับ | **ไทย** | ของที่คนอ่าน |

ผลพลอยได้ที่สำคัญกว่าที่คิด: ฐานข้อมูลเดิมทั้งก้อนยังใช้ได้ ไม่ต้องแปลย้อนหลัง ·
`emotion.label` ในฐานยังสะกดตาม FER2013 **ซึ่งเป็นเหตุผลทั้งหมดที่เลือกสะกดแบบนั้น**
(เทียบกับ FER ของทีมคุณสุชาติได้ตรงๆ) แปลตั้งแต่ในฐาน = ทิ้งข้อดีนั้นไปเลย ·
อยากได้อังกฤษคืนวันไหน แก้ไฟล์เดียว

**ที่ไม่แปล และเหตุผล**

`status` `image.source` `id` `ref` `group.ref` `request_id` `camera_id` เป็น
**ค่าโปรโตคอล ไม่ใช่คำบรรยายคน** ปลายทางเขียน `if status == "ok"` ไว้แล้ว
แปลเมื่อไหร่โค้ดเขาพังเงียบๆ ในวันที่ทุกอย่างดูปกติดี

`uniform.text` คือข้อความที่**ลอกมาจากชุดจริง** ("SECURITY") มันคือหลักฐาน
ไม่ใช่ค่าที่เราตีความ แปลแล้วไม่ใช่หลักฐานอีกต่อไป

`age_range` (`20-29`) กับ `emotion.valence` เป็นตัวเลข แปลแล้วเสีย sort/compare ไปเปล่าๆ
ส่วน `description` `where` `reason` `distinctive` เป็นไทยอยู่แล้วตั้งแต่ prompt

**`response_model=PersonsOut` ถูกถอดจาก decorator ของสอง POST** เพราะค่าไทยไม่ผ่าน
`Literal` ใน schema ซึ่งถูกต้องแล้ว: schema คือความจริงฝั่งใน ไม่ใช่รูปของสายที่ส่งออก
ปล่อยไว้ = FastAPI validate ซ้ำแล้วตก 500 ทั้งที่คำตอบถูกทุกช่อง

**หน้า `/people/verify/` ถอดตารางแปลของตัวเองทิ้งทั้งชุด** มันเคยมีตาราง
อังกฤษ→ไทย ครบทุกฟิลด์ พอ API แปลให้แล้ว ตารางนั้นกลายเป็นตารางแปลชุดที่สองทันที
ตอนนี้หน้าเว็บพิมพ์ค่าที่ได้มาตรงๆ เหลือแต่ตารางสี/ไอคอนซึ่งต้องคีย์ด้วยคำไทย
และมี `test_คำไทยในหน้า_verify_ต้องตรงกับ_people_th` เฝ้าไม่ให้สองที่หลุดจากกัน
**ถ้าไม่มีด่านนี้ อาการตอนหลุดคือป้ายเทาหมด ไอคอนเป็นเครื่องหมายคำถามหมด
โดยหน้ายังโหลดปกติ ไม่มี error สักบรรทัด**

`test_ทุกค่าใน_schema_ต้องมีคำแปลไทย` ไล่จาก Literal ใน `schemas.py` เป็นตัวตั้ง
เพิ่มค่าใหม่แล้วลืมเติมคำแปล = ตกทันที ไม่ใช่หลุดเป็นอังกฤษปนไทยไปให้ปลายทางเจอเอง

⚠️ **ต้องบอกทีมคุณสุชาติก่อนใช้** ฟิลด์ไม่มีตัวไหนหาย ไม่มีตัวไหนเปลี่ยนชื่อ
แต่ **ค่าของ `direction` กับ `gender` ที่เขาอาจ parse อยู่แล้ว เปลี่ยนภาษา**
ยังไม่เคย confirm รูป JSON กับเขา เลยยังแก้ได้ แต่หลังจากนี้ต้องตกลงก่อน

### สิ่งที่คำนวณเองได้ ห้ามถามโมเดล

`age_group` มาจาก `age_range` ในโค้ด · `group.size` มาจากการนับ `group.ref` ที่ซ้ำกัน
**ถามสองครั้งเรื่องเดียวกัน = วันหนึ่งจะได้คำตอบที่ขัดกันเอง** แล้วปลายทางไม่มีทาง
รู้ว่าเชื่อช่องไหน · โมเดลแปะป้ายกลุ่มเหมือนกันให้สองคนแล้วเขียน `size: 3`
ในคำตอบเดียวกันได้สบายๆ เรื่องเดียวกับที่ฝั่งช้างให้ CV นับ ไม่ให้ VLM นับ
ผลพลอยได้คือไม่เสีย token เพิ่มสักตัว

`emotion` ก็ใช้หลักเดียวกันครึ่งทาง: โมเดลตอบ `{"label":"happy","valence":1}` ได้
**เราเชื่อ `label` แล้วดึงเลขกลับเข้าช่วงของ label นั้น** ไม่ใช่ทิ้งทั้งก้อน
(label คือสิ่งที่มันมองเห็น ส่วนตัวเลขคือสิ่งที่มันต้องแปลงเอง ซึ่งพลาดบ่อยกว่า)

### 🔴 `group` มีจริงเฉพาะเส้น `/v1/frames`

เส้น `/v1/persons` ยิงโมเดลทีละคนจาก crop ของคนนั้น **โมเดลไม่มีทางรู้ว่าข้างๆ มีใคร**
จึงเป็น `unknown` เสมอ ไม่ใช่ `alone` · **"อยู่คนเดียวในภาพที่เราส่งให้ดู" ไม่ได้แปลว่า
"เดินคนเดียว"** ตอบ alone ที่นั่นคือการโกหกว่าเรารู้ ทั้งที่เราแค่ไม่ได้ดู

อยากได้ group ที่เส้น persons ต้องคิดจากระยะของ bbox ในเฟรมเดียวกัน ซึ่งเป็น
คนละเรื่อง ("ยืนใกล้กัน" ≠ "มาด้วยกัน") และยังไม่มีใครสั่ง

### 🔴🔴 สัญชาติ: ทำให้แล้ว แต่ **ปิดอยู่** (`PEOPLE_ALLOW_NATIONALITY=false`)

ทำแบบเดียวกับ `model` ที่ถูกซ่อน: **คีย์อยู่ในคำตอบเสมอ รูป response ไม่เปลี่ยน
ตามค่า env** แต่ค่าเป็น `unknown` และ **prompt ไม่ถามเรื่องนี้เลย** จนกว่าจะเปิด
ปิดเป็นสองชั้น (prompt ไม่ถาม + `normalize` บังคับ unknown) เพราะด่านชั้นเดียว
คือด่านที่พังเงียบวันที่มีคนแก้ prompt แล้วลืมสวิตช์

เหตุผลที่ไม่เปิดให้เอง:
1. **ภาพบอกสัญชาติไม่ได้** สัญชาติอยู่ในพาสปอร์ต ไม่ได้อยู่บนหน้า สิ่งที่โมเดลทำได้
   คือเดาว่า "หน้าตาคล้ายคนแถบไหน" ซึ่งคนละเรื่องกันคนละชั้น · ไทย ลาว พม่า มาเลย์
   แยกจากภาพ CCTV ไม่ได้และไม่มีวันแยกได้ ชุดค่าจึงเป็นระดับแถบ ไม่ใช่ระดับประเทศ
2. **ผิดแบบที่ตรวจไม่ได้** โมเดลไม่เคยตอบว่าไม่รู้ มันตอบทุกใบด้วยน้ำเสียงมั่นใจ
   เท่ากันหมด · "เสื้อขาว" ที่ผิด เปิดภาพดูก็รู้ทันที แต่สัญชาติที่ผิด
   **ถือภาพอยู่ในมือก็ตรวจไม่ได้**
3. เอกสารโครงการเขียนห้ามไว้เองตั้งแต่วันแรก ("ไม่เดาชื่อ สัญชาติ เชื้อชาติ")
   และอาคารที่ติดกล้องอาจมีนโยบายของเขาเอง (PDPA)

`GET /people/healthz` จึงคืน `nationality_enabled` มาด้วย ไม่ซ่อนตาม `EXPOSE_MODEL`
เพราะมันไม่ใช่ข้อมูลโมเดล มันคือ "คำตอบที่คุณได้รับแปลว่าอะไร" ซึ่งปลายทางต้องรู้เสมอ
ไม่งั้นเขาจะนั่งไล่หาว่าทำไมฟิลด์นี้ไม่เคยมีค่า แล้วโทษโมเดลทั้งที่เราปิดไว้เอง

เปิดเมื่อไหร่ ให้เก็บ `/truth` ควบไปด้วยตั้งแต่วันแรก **ตอนนี้ความแม่นของฟิลด์นี้คือ
"ไม่รู้" ไม่ใช่ "พอใช้"**

### 🔴🔴 ทำไมบางภาพโมเดลตีความแปลก (คำถามจาก Toy 2026-09-12)

เปิดภาพตัวอย่างสองใบดูจริง **คำตอบคือมันเห็นทั้งภาพ และนั่นแหละคือปัญหา**

`iplus_sample1.jpg` **ไม่ใช่เฟรมกล้อง มันคือภาพถ่ายหน้าจอ dashboard ของทีมเขา**
ในพิกเซลมีครบ: แถบข้อความบนสุด "เข้า 11 ออก 5 อยู่ในตึก 6" · ปุ่ม play กลางจอ ·
กรอบเหลืองพร้อมป้าย ID:011/021/022 วาดทับ · และ **รูปหน้าคนสองใบแปะมุมขวา
ซึ่งคือคนสองคนเดิมในเฟรมนั้นเอง** โมเดลอ่านทั้งหมดเป็นเนื้อหาของภาพ มันกำลัง
บรรยายรูปของจอมอนิเตอร์ ไม่ใช่บรรยายฉากที่กล้องเห็น

`iplus_sample2.jpg` เป็นเฟรมจริง แต่มีกรอบ เขียว/แดง/น้ำเงิน ฝังในพิกเซล
prompt เดิมไม่เคยบอกว่ากรอบพวกนั้นคืออะไร โมเดลจึงอ่านว่า "นี่คือรายชื่อคนในภาพ"
แล้วตอบ 3 คน ทั้งที่มี **รปภ.ชุดเขียวคำว่า SECURITY** ยืนอยู่หลังประตู คนชุดชมพู
และคนเดินนอกอาคารอีกหลายคน

สามเหตุ เรียงตามน้ำหนัก:
1. **overlay ที่ฝังมาในภาพ** (แก้แล้วใน f2/p2: บอกให้มันรู้จักกรอบ ป้าย ตัวนับ
   ปุ่ม และรูปย่อมุมจอ ว่าเป็นของที่ระบบอื่นวาดทับ ไม่ใช่เนื้อหา)
2. **เส้น `/v1/persons` มันไม่เห็นทั้งภาพจริงๆ** เห็นแค่ crop + margin 8%
   ซึ่งแปลว่า `height` ที่สั่งให้ "เทียบกับคนอื่นในเฟรม" **ไม่มีอะไรให้เทียบเลย**
   p2 เลยบอกตรงๆ ว่าใน crop ส่วนสูงปกติคือ unknown
3. **ความละเอียด** provider ย่อภาพลงราว 1024px ก่อนเข้าโมเดล คนที่ยืนไกล
   ในเฟรม 1916x1080 เหลือ 20-30px ซึ่งอ่านอะไรไม่ได้จริงๆ อันนี้แก้ที่ prompt ไม่ได้

**ทางแก้ที่ดีที่สุดไม่ได้อยู่ฝั่งเรา: ขอเฟรมดิบจากทีมคุณสุชาติ** ไม่ใช่ screenshot
ของจอเขา · ที่เราทำได้คือสอนโมเดลให้รู้จัก overlay ซึ่งทำไปแล้ว

### 🔴🔴 วัดจริงบน prod 2026-09-12 · กรอบที่วาดฝังในภาพ กดคนที่อยู่นอกกรอบหายไป

**นี่คือการทดลองที่ตัดสินเรื่องนี้จบ ไม่ใช่ข้อสันนิษฐานอีกต่อไป**

ยิง `iplus_sample2.jpg` เข้า `/v1/frames` บน prod สามรอบ prompt คนละเวอร์ชัน:

| | f1 | f2 (เพิ่มกฎ overlay เป็น bullet) | f3 (ทำกฎเป็นขั้นตอนที่ 1) | **f3 + ลบกรอบออกจากพิกเซล** |
|---|---|---|---|---|
| คนที่เจอ | 3 | 3 | 3 | **5-6** |
| `in_uniform` | - | 0 | 0 | **1** |
| `uniform_text` | - | `[]` | `[]` | **`['SECURITY']`** |
| `groups_found` | - | 1 | 1 | **3** |
| `emotion_unknown` | - | 0 | 0 | **2** (คนหันหลัง ถูกต้อง) |
| `direction_out` | - | 0 | 0 | **2** (เดินออก ถูกต้อง) |

**แก้ถ้อยคำใน prompt สองรอบ ตัวเลขไม่ขยับสักช่อง · ลบกรอบออกจากภาพ ขยับทุกช่อง**

และไม่ใช่เรื่องความละเอียด **พิสูจน์แยกแล้ว**: ชี้ `bbox` ของ รปภ. เข้า `/v1/persons`
ตรงๆ โมเดลตอบ `uniform.kind = security` และลอกคำว่า `SECURITY` จากหลังเสื้อมาได้เป๊ะ
พร้อม `direction = out` และ `emotion = unknown/0` (หันหลัง) ถูกทุกช่อง
**มันเห็นเขา มันแค่ไม่ใส่เขาในรายชื่อตอนมีกรอบวาดอยู่ในภาพ**

🔴 **ทางแก้อยู่ที่ต้นทาง ขอเฟรมดิบจากทีมคุณสุชาติ** ไม่ใช่ภาพที่วาดกรอบทับแล้ว
ระบบเขามีเฟรมดิบอยู่แล้วแน่นอน เพราะ detector ของเขาต้องกินเฟรมดิบเข้าไปก่อนจะวาดกรอบ

⚠️ **ห้ามแก้ด้วยการลบกรอบฝั่งเรา** ที่ผมทำคือถมพิกเซลสีอิ่มตัวสูงทิ้ง ซึ่งจูนมากับ
ภาพใบนี้ใบเดียว · เอาขึ้น prod เมื่อไหร่ วันแรกที่มีคนใส่เสื้อแดงสดเดินเข้ามา
เราจะถมตัวเขาทิ้งเอง แล้วไม่มีใครรู้ **ตัวกรองที่จูนกับภาพใบเดียว คือบั๊กที่รอเวลา**
ภาพหลักฐานของการทดลองนี้อยู่ที่ `outputs/_sample2_noboxes.jpg`
กับ `outputs/_guard_check.png`

⚠️ **ที่ยังไม่ดี แม้ลบกรอบแล้ว** โมเดลจับสามคนที่เดินเข้ามาเป็นกลุ่มเดียวกัน (G1)
ทั้งที่หน้าตาเหมือนคนละคนละกลุ่มมาพร้อมกัน · `group` ยังเอียงไปทาง "อยู่ใกล้กัน = มาด้วยกัน"
ทั้งที่ prompt สั่งตรงข้ามไว้แล้ว ต้องมี `/truth` ถึงจะรู้ว่าผิดจริงไหม

และ **6 คนใช้เวลา 40 วินาที** บนเส้น frames ซึ่งนานเกินสำหรับระบบหน้าประตู
เวลาขึ้นตามจำนวนคนที่เจอ ยิ่งเจอครบยิ่งช้า อันนี้เป็นราคาที่ต้องคุยกับปลายทาง

### ⚠️ ตัวเลขจะขยับ และยัง**ไม่ได้ยิงซ้ำ**

prompt p1 -> **p2** · f1 -> **f2** · แตะกฎเรื่อง "ใครนับเป็นคน" ตรงๆ
วัดมาแล้วรอบก่อนว่าแค่เพิ่มคำถามเรื่องทิศ คนใน sample1 ขยับจาก 2 เป็น 3
รอบนี้จะขยับอีกแน่ และน่าจะขยับขึ้น (เพราะสั่งให้มองคนนอกกรอบด้วย)

**ยิงซ้ำบน prod แล้ว 2026-09-12** (เครื่อง dev รัน qwen3-vl ไม่ไหว Toy เลยสั่งให้
ทดสอบบน DO ตรงๆ) ผลอยู่ในหัวข้อข้างบน · เครื่อง dev ยังไม่มีโมเดลเหมือนเดิม
LM Studio ไม่ได้เปิด และ `.env` ยังเป็น `LLM_API_KEY=not-needed`
มี `tools/people_regression.py` ไว้ให้ยิงชุดเดิมแล้วเทียบเป็นตาราง:

```
python tools/people_regression.py --save after.json        ../../inbox/smart_people_counting/iplus_sample*.jpg
```

**ตัวเลขที่ขยับไม่ได้แปลว่าดีขึ้น มันแปลว่าเปลี่ยน** ใครถูกมาจาก `/truth` เท่านั้น

### ราคาที่จ่ายเพิ่ม (ประมาณ ยังไม่ได้วัดจริง)

ตัวอักษรใน prompt: p1 +48% · f1 +64% (เปิดสัญชาติด้วยเป็น +63% / +77%)
ตัวที่แพงสุดคือกฎเรื่อง overlay ซึ่งเป็นตัวที่แก้ปัญหาจริง เลยไม่ตัด

`PEOPLE_LLM_MAX_TOKENS` 700 -> **850** · `PEOPLE_FRAME_MAX_TOKENS` 2500 -> **3500**
🔴 ตัวหลังจำเป็น ไม่ใช่เผื่อเหนียว: uniform + emotion + group ทำให้เป็นราว 240 tok/คน
240 x 12 = 2,880 ซึ่ง**ทะลุ 2500 เดิม** ถ้าไม่ขยับพร้อม prompt เฟรมที่คนเยอะจะโดน
`finish=length` แล้ว**เสียทั้งเฟรม** โดยอาการที่เห็นคือ degraded ลอยๆ ไม่ชี้มาที่
prompt ที่เพิ่งแก้เลย

เดาว่าต้นทุนต่อคนขยับจาก ~$0.00022 เป็นราว **$0.00026 (+20%)** ตัวเลขนี้
**ยังไม่ได้วัด** ต้องยิง OpenRouter จริงแล้วดู `completion_tokens` ใน DB

### ฐานข้อมูล

`persons` เพิ่ม 11 คอลัมน์ (`age_group` `nationality*` `emotion_*` `group_*`
`uniform_kind` `uniform_text`) ผ่าน `_migrate()` เหมือนเดิม ทดสอบกับไฟล์เก่าแล้ว
แถวเก่าไม่หาย · **`uniform_kind` / `uniform_text` ซ้ำกับที่อยู่ใน `appearance` JSON
โดยตั้งใจ** สองช่องนี้คือของที่ปลายทางจะ GROUP BY จริง ("เดือนนี้ รปภ. เข้ากี่คน")
ขุดออกจาก JSON ทุก query คือช้าและเขียน SQL ยาก · ที่เหลือไม่มีใครถาม เลยไม่แตกทั้งก้อน

### ด่านที่เพิ่มมา

`test_ชุดค่าในโค้ดกับใน_prompt_ต้องตรงกันทุกตัว` ไล่จาก **schema เป็นตัวตั้ง**
เทียบกับ `prompt_p1.ALLOWED` ทุกฟิลด์ · เพิ่มค่าใน `schemas.py` แล้วลืมเติมใน prompt
= ตกทันที ไม่ต้องรอให้ใครสังเกตว่าคำตอบมันแปลกๆ
**นี่คือด่านที่ควรมีตั้งแต่วันที่ `laptop` หายไป แต่ตอนนั้นแก้เฉพาะเคส**

### บั๊กที่เจอตอนทำ (เจ็บและเงียบ)

สร้าง index บน `client_request_id` ไว้ใน `SCHEMA` ตรงๆ แล้ว **ฐานข้อมูลเก่าเปิดไม่ขึ้นทั้งไฟล์**
เพราะ `executescript` สร้าง index ก่อนที่ `_migrate()` จะได้ ALTER TABLE เติมคอลัมน์
`CREATE TABLE IF NOT EXISTS` ข้ามตารางเก่า แต่ `CREATE INDEX` ไม่ข้ามให้
**index ต้องมาหลังคอลัมน์เสมอ ย้ายไปสร้างใน `_migrate()` แทน**

## `mobility` เด็กในรถเข็น (v0.7.0 · 2026-09-14)

Toy ทักสองเรื่องในประโยคเดียว: **"ที่สั่งไปแก้เฉพาะ persons ไม่ได้แก้ frames
หรือเปล่า"** กับ **"กรณีเป็นเด็ก อยากให้แยกว่าเป็นเด็กในรถเข็นไหม"**

### เรื่องแรก ตรวจแล้ว · ของเก่าขึ้นครบทั้งสองเส้น แต่คำถามถูกต้อง

ไล่เทียบ prompt สองตัวทีละ bullet แล้ว: uniform · emotion · overlay · demographic
ขึ้นครบทั้ง p1 และ f1 · ที่ต่างกันมีสี่ข้อและ**ทุกข้อมีเหตุผลเขียนไว้ก่อนแล้ว**
(`group`/`where` มีเฉพาะ frames · กฎเรื่องส่วนสูงกับ overlay เขียนคนละแบบเพราะ
เส้นหนึ่งเห็น crop อีกเส้นเห็นทั้งเฟรม)

**แต่อาการที่ Toy เห็นเป็นเรื่องจริง**: ชี้ bbox ของ รปภ. เข้า `/v1/persons`
แล้วอ่านออกครบ ยิงเฟรมเดียวกันเข้า `/v1/frames` แล้ว รปภ. หายไป
สาเหตุอยู่ในหัวข้อ "กรอบที่วาดฝังในภาพ กดคนที่อยู่นอกกรอบหายไป" ข้างบน
ซึ่งวัดมาแล้วว่า **prompt แก้ไม่ได้ ต้องขอเฟรมดิบ** ไม่ใช่เพราะลืมแก้เส้น frames

🔴 เจอของจริงหนึ่งอย่างตอนไล่: **commit 2cf112f แก้ถ้อยคำใน `prompt_p1` จริง
แต่ไม่ได้ขยับ `PROMPT_VERSION`** (f1 ขยับเป็น f3 ในคอมมิตเดียวกัน)
แถวในฐานที่ป้ายว่า `p2` จึงมาจาก prompt สองตัว แยกกันด้วยเวลาอย่างเดียว
เขียนเตือนไว้ใน docstring ของ `prompt_p1` แล้ว · รอบนี้ขยับครบทั้งคู่ (p3 · f4)

### ของใหม่: `mobility` ระดับคน ทั้งสองเส้น

| ค่า | ไทย | หมายถึง |
|---|---|---|
| `walking` | เดินเอง | ยืน/เดินด้วยขาตัวเอง · ไม้เท้า ไม้ค้ำ วอล์กเกอร์ ยังนับ |
| `stroller` | อยู่ในรถเข็นเด็ก | เด็กเล็กนั่งอยู่ในรถเข็น |
| `wheelchair` | นั่งรถเข็นวีลแชร์ | ทุกช่วงอายุ |
| `carried` | ถูกอุ้ม | อุ้ม สะพาย เป้อุ้มเด็ก |
| `other` | อื่นๆ | จักรยาน สกู๊ตเตอร์ |
| `unknown` | ไม่ทราบ | **มองไม่เห็นช่วงล่าง** |

`summary` เพิ่ม `mobility_*` หกตัว (บวกครบจำนวนคน มีถัง unknown เหมือนทุกชุด)
บวก `child_in_stroller` ซึ่งเป็นการ **ไขว้สองช่อง** (เด็ก x รถเข็นเด็ก)
ไม่ใช่สมาชิกของชุดไหน · **ห้ามเอาไปบวกรวม** มันนับซ้ำกับทั้ง `age_child`
และ `mobility_stroller` โดยตั้งใจ

🔴 **ช่องนี้เป็นของทุกคน ไม่ใช่ของเฉพาะตอน `age_group = child`** สามเหตุผล
เรียงตามน้ำหนัก: ฟิลด์ที่โผล่มาตามค่าช่องอื่นคือฟิลด์ที่ปลายทาง parse พัง ·
**ผู้สูงอายุนั่งวีลแชร์เข้าอาคารทุกวัน** และ รปภ. อยากรู้พอกัน ·
อายุที่อ่านจากภาพ CCTV พลาดได้ ผูกไว้กับ `age_group` เมื่อไหร่ เด็กที่ถูกอ่าน
เป็น 13-19 จะทำให้ "เด็กในรถเข็น" หายไปทั้งแถว

🔴 **คนเข็นไม่ใช่คนถูกเข็น** แม่ที่เข็นรถเข็น = `walking` · เด็กในรถเข็น = อีกคนหนึ่ง
ที่เป็น `stroller` · กฎนี้อยู่ในทั้งสอง prompt และมีเทสต์เฝ้า ถ้าหายไป
ยอด "เด็กในรถเข็น" จะเบิ้ลหรือหายทั้งแถว แล้วแต่ว่ามันยุบไปทางไหน

⚠️ **ที่หน้าประตูจริง `mobility_unknown` จะเป็นถังที่ใหญ่ที่สุด** เพราะช่วงล่าง
ของตัวคนถูกเคาน์เตอร์กับคนข้างหน้าบังบ่อยที่สุดในภาพ · นั่นคือคำตอบที่ถูก
**ถ้าวันไหน unknown เป็นศูนย์ทั้งวัน แปลว่าโมเดลเดาแทนที่จะตอบว่ามองไม่เห็น**

### 🔴🔴 วัดจริงบน prod 2026-09-14 · f4 เจอ รปภ. ทั้งที่กรอบยังอยู่ในภาพ

**ผลนี้ขัดกับข้อสรุปของวันที่ 12 ก.ย. ซึ่งเขียนไว้ข้างบนว่า "prompt แก้ไม่ได้"**

ยิง `iplus_sample2.jpg` (ภาพเดิม กรอบสี **ยังฝังอยู่ในพิกเซลครบ** ไม่ได้ลบอะไรออก)
เข้า `/v1/frames` บน prod ด้วย f4 สองรอบ:

| | f1 | f2 | f3 | **f4 (2026-09-14)** | f3 + ลบกรอบออกจากพิกเซล |
|---|---|---|---|---|---|
| คนที่เจอ | 3 | 3 | 3 | **6 · 5** | 5-6 |
| `in_uniform` | - | 0 | 0 | **1** | 1 |
| `uniform_text` | - | `[]` | `[]` | **`['SECURITY']`** | `['SECURITY']` |
| `groups_found` | - | 1 | 1 | **3** | 3 |

**f4 ได้ตัวเลขชุดเดียวกับตอนที่ลบกรอบออกจากภาพ โดยที่กรอบยังอยู่ครบ**
รปภ.ชุดเขียวหลังประตูที่หายมาตลอดสามเวอร์ชัน กลับมาอยู่ในรายชื่อ พร้อมคำว่า
`SECURITY` ที่ลอกจากหลังเสื้อ และ `where` บอกว่า "outside the glass doors"

สิ่งเดียวที่ f4 เพิ่มในขั้นตอนที่หนึ่งคือประโยคเรื่องเด็กในรถเข็นกับเด็กที่ถูกอุ้ม
ซึ่งลงท้ายว่า **"each of them gets a row of their own, separate from the adult
pushing or holding them"** · เดาว่าประโยคนี้ทำงานเพราะมันเป็น**ตัวอย่างรูปธรรมของคน
ที่ต้องนับทั้งที่ไม่มีกรอบล้อมรอบ** ไม่ใช่กฎนามธรรมว่า "อย่าสนใจกรอบ"
บทเรียนเดิมของงานนี้ซ้ำอีกครั้ง: กฎที่โมเดลทำตามคือกฎที่เป็นขั้นตอนและมีตัวอย่าง

⚠️ **สองรอบได้ 6 กับ 5 ไม่เท่ากัน** และยังไม่มี ground truth ว่าจริงๆ มีกี่คน
**"ตัวเลขที่ขยับไม่ได้แปลว่าดีขึ้น มันแปลว่าเปลี่ยน"** ยังใช้อยู่ทุกตัวอักษร
สิ่งที่พูดได้จริงคือ: รปภ. คนนั้นเข้ามาอยู่ในรายชื่อแล้ว ซึ่งก่อนหน้านี้ไม่เคยเข้า

🔴 **ยังไม่ถอนคำขอเฟรมดิบ** ขอต่อไปเหมือนเดิม เพราะ (ก) ตัวเลขยังแกว่ง
(ข) ที่ได้มาคือผลจากประโยคที่เขียนไว้เพื่อเรื่องอื่นทั้งหมด ซึ่งวันหนึ่งอาจถูกแก้
แล้วอาการกลับมาโดยไม่มีใครโยงถูก (ค) เฟรมดิบยังถูกกว่าและแม่นกว่าอยู่ดี

### ⚠️ `iplus_sample1` ยังแกว่ง และรอบหนึ่งตอบ **0 คน**

สามรอบบน f4 ได้ **0 · 3 · 3** · รอบที่ได้ 0 นั้น `status` เป็น `ok` และ `reason` ว่าง
ซึ่งอ่านได้ว่า "ล็อบบี้ว่าง" ทั้งที่ในภาพมีคนเดินอยู่

**นี่คือเคสที่แย่กว่าการนับขาด** เพราะปลายทางแยกไม่ออกเลยว่าเป็นภาพที่ไม่มีคน
กับภาพที่เราอ่านพลาด · ไม่แก้ด้วยการห้ามตอบศูนย์ (ล็อบบี้ว่างจริงก็มี)
แต่ต้องรู้ว่ามันเกิดได้ และ **ห้ามเอาเลขจากภาพเดียวรอบเดียวไปสรุปอะไร**
sample1 เป็นภาพถ่ายหน้าจอ dashboard ซึ่งเป็นชนิดภาพที่แย่ที่สุดสำหรับงานนี้อยู่แล้ว

### mobility บน prod · ทุกคนได้ `เดินเอง` และ `unknown` เป็นศูนย์

หกคนใน sample2 และสามคนใน sample1 ได้ `เดินเอง` หมด ความมั่นใจ 0.7-0.9
ซึ่งดูสมเหตุสมผลกับภาพ (ล็อบบี้โล่ง เห็นเต็มตัวทุกคน ไม่มีรถเข็นสักคัน)

⚠️ **แต่ `mobility_unknown = 0` ทั้งสองภาพ คือสิ่งที่ต้องจับตา** ตามที่เขียนไว้ข้างบนเอง
ว่าถ้าวันไหน unknown เป็นศูนย์ทั้งวันแปลว่าโมเดลเดา · สองภาพนี้ยังตัดสินไม่ได้
เพราะมันเห็นเต็มตัวจริงๆ **ภาพที่จะตัดสินเรื่องนี้คือภาพที่มีเคาน์เตอร์บังครึ่งตัว
ซึ่งเรายังไม่มี** และยังไม่เคยเห็นเด็กในรถเข็นสักภาพเดียว
**ฟีเจอร์นี้จึงยังไม่ถูกทดสอบในสิ่งที่มันถูกสร้างมาเพื่อทำ** ต้องขอภาพจากทีมคุณสุชาติ

---

# งานที่สาม — อ่านป้ายทะเบียนไทย (`/lpr/*`)

เพิ่ม 2026-09-14 ตามที่ Toy สั่ง: **repo เดียวกัน แยก endpoint แยกฐานข้อมูล**
งานช้างกับงานคนไม่ถูกแตะเลย นอกจากสามบรรทัดท้าย `app/main.py` ที่ mount เข้าไป
(และสองคีย์ใน `/healthz` ที่บอกว่า mount ติดไหม)

คำสั่งเต็ม: *"clone โปรเจคเป็น license plate LPR ภาษาไทย ... endpoint เป็น
lpr/v1/vehicle เพื่ออ่านทะเบียนออกมาโดยแยกเป็น ตัวอักษร ประเภทรถ (รถยนต์
จักรยานยนต์ รถบรรทุก) กรณีเห็นตัวรถด้วย และจังหวัด และค่า confidence
ดูก่อนครับ แล้วเราค่อยมาแก้กัน"*

**นี่คือ v0.1.0 ที่ยังไม่เคยเจอภาพจริงสักใบ** ทุกตัวเลขในหัวข้อนี้เป็นค่าตั้งต้น
ที่ยังไม่ได้วัด · อ่านหัวข้อ "ยังไม่ได้วัด" ท้ายสุดก่อนเชื่ออะไร

## endpoint

| method | path | ทำอะไร |
|---|---|---|
| POST | `/lpr/v1/vehicle` | เส้นหลัก · หนึ่งภาพ ตอบทุกป้ายที่อ่านได้ |
| GET | `/lpr/v1/vehicle/{request_id}` | ดูผลย้อนหลัง |
| GET | `/lpr/v1/vehicle/by-ref/{id}` | ตามผลด้วยเลขอ้างอิงของปลายทางเอง |
| POST | `/lpr/v1/vehicle/{request_id}/{ref}/truth` | ทะเบียนจริงที่คนตรวจแล้ว |
| GET | `/lpr/v1/accuracy` | อ่านถูกกี่แถวจากแถวที่ตรวจแล้ว |
| POST | `/lpr/v1/maintenance/prune` | ลบของเกินอายุ (cron ข้างนอก) |
| GET | `/lpr/healthz` · `/lpr/verify/` | สถานะ · หน้าทดสอบด้วยตา |

`/healthz` ของฝั่งช้างเพิ่มสองฟิลด์ `lpr_mounted` / `lpr_error` เท่านั้น
รูป JSON ของงานช้างและงานคน ไม่ขยับสักตัว (เทสต์เดิมเฝ้าไว้อยู่)

## request · body เหมือนงานคนเป๊ะ ตั้งใจ

```jsonc
{"camera_id":"gate-1","image_base64":"...",
 "bbox":[795,95,180,60],            // ไม่บังคับ · กรอบป้ายหรือกรอบรถจาก ALPR ปลายทาง
 "ts":"...","note":"...","client_request_id":"gate-0001"}
```

ปลายทางที่ยิงงานคนอยู่แล้ว เปลี่ยนแค่ path · ไม่ต้องเรียนรูปที่สาม
ส่ง `bbox` มา = เราครอปให้ตรงก่อนเข้าโมเดล แล้วคืน `image.bbox_used` กลับไป
ซึ่ง**ไม่ใช่ค่าเดียวกับที่ส่งมา** (เผื่อขอบ 10% แล้วหนีบขอบภาพแล้ว)

## response

```jsonc
{"request_id":"...","ref":"gate-0001","camera_id":"gate-1","status":"ok",
 "vehicles":[{
   "ref":"V1","status":"ok","where":"กลางภาพ หันหน้าเข้ากล้อง",
   "plate":{"text":"1กข 1234","text_raw":"1กข 1234",
            "prefix":"1","letters":"กข","digits":"1234",
            "color":"ขาว","confidence":0.85},
   "province":"สงขลา","province_confidence":0.6,
   "vehicle_type":"รถกระบะ","vehicle_type_confidence":0.9,
   "vehicle_color":"ขาว",
   "description":"รถกระบะสีขาว ทะเบียน 1กข 1234 สงขลา",
   "overall_confidence":0.78,"reason":""}],
 "summary":{"vehicles_found":1,"plates_read":1,"plates_unread":0,
            "province_known":1,"province_unknown":0,"type_pickup":1,...},
 "image":{"w":640,"h":360,"source":"full_image","bbox_used":null},
 "model":null,"timing_ms":{...}}
```

ค่าเป็นไทย คีย์เป็นอังกฤษ เหมือนงานคน · **ยกเว้นทะเบียนกับจังหวัด ซึ่งเป็นหลักฐาน
ห้ามแตะ** และ `status`/`ref` ซึ่งเป็นค่าโปรโตคอล

## สามข้อที่ตัดสินใจต่างจากที่สั่งมาตรงๆ และเหตุผล

**1. โมเดลตอบ `plate_text` พวงเดียว · เราแยก ตัวอักษร/ตัวเลข/เลขนำหน้า เองในโค้ด**
Toy สั่งว่า "แยกเป็นตัวอักษร" ซึ่งได้ผลลัพธ์ตามสั่งครบทุกช่อง แต่วิธีที่จะพัง
คือไปถามโมเดลแยกเป็นช่องๆ แล้ววันหนึ่งจะได้ `letters: "กข"` คู่กับ
`text: "กจ 1234"` ในคำตอบเดียวกัน แล้วปลายทางไม่มีทางรู้ว่าเชื่อช่องไหน
หลักเดียวกับ `age_group` และ `group.size` ของงานคน ไม่ใช่กฎใหม่

**2. จังหวัดเป็นชุดปิด 77 ตัว · ชื่อนอกชุดคืนค่าว่าง ไม่ใช่ค่าที่ใกล้ที่สุด**
🔴 **ห้ามใส่ fuzzy match เด็ดขาด** "ตาก" กับ "ตราด" ต่างกันหนึ่งตัวอักษร
และจังหวัดที่ผิดคือรถคนละคันในฐานทะเบียน เหมือนทะเบียนที่ผิด
ที่รับคือคำ**ย่อ**ที่คนไทยใช้แทนชื่อเต็มตัวเดียวกันจริงๆ (กทม. · อยุธยา)
ซึ่งคนละเรื่องกับการเดาว่า "น่าจะหมายถึงจังหวัดนี้"

**3. ไม่มีฟิลด์ "ประเภทป้าย" (ส่วนบุคคล/สาธารณะ/ป้ายแดง) มีแต่ `plate.color`**
สีป้ายคือหลักฐานที่กล้องเห็น · การแปลงสีเป็นประเภทคือความรู้เรื่องระเบียบขนส่ง
ที่เราไม่ได้ตรวจสอบมา และมันเปลี่ยนได้โดยที่โค้ดเราไม่รู้ตัว
ปลายทางซึ่งทำงานกับทะเบียนรถทุกวัน เป็นคนแปลเอง
(หลักเดียวกับ `uniform` ของงานคน: เราบอกว่าเห็นชุดแบบไหน ไม่บอกว่าเขาทำอาชีพอะไร)

## กติกาที่ยืมมาจากสองงานก่อนหน้าทั้งดุ้น

1. **โมเดลล่ม = `degraded` พร้อมรายชื่อว่าง ห้ามตอบ `ok` ที่ศูนย์คัน**
   "ไม่มีป้ายในภาพ" กับ "อ่านภาพไม่ได้" หน้าตาเหมือนกันเป๊ะถ้ายุบเป็นอันเดียว
   แล้วปลายทางจะบันทึกว่า "ไม่มีรถผ่านประตู" ในนาทีที่ระบบเราล่ม
2. **`finish=length` ห้ามรับมาใช้** ทะเบียนที่ถูกตัดท้าย คือทะเบียนของรถคันอื่น
3. **บันทึก request ลงฐานก่อนยิงโมเดล** ไม่ใช่หลังได้คำตอบ
4. **ไม่ขอ bbox จากโมเดล** วัดมาแล้วฝั่งช้าง คืน y=998 บนภาพสูง 628
   ตำแหน่งขอเป็นข้อความใน `where` ซึ่งเป็นสิ่งที่มันทำได้จริง
5. **ทุกชุดใน summary บวกครบจำนวนคัน และมีถัง unknown**
6. **ไม่ส่ง base64 เข้า LangSmith trace**
7. **ไม่เก็บภาพเป็นค่าเริ่มต้น**

## ข้อที่ **ไม่ได้**ยืมมา และเป็นข้อที่ต่างจากงานคนมากที่สุด

งานบรรยายคน "เสื้อขาว" ที่ผิด ยังพอใช้ตามตัวได้ · งานนี้ **ทะเบียนที่ผิดหนึ่งตัว
ชี้ไปที่รถคนละคัน** ไม่ใช่ข้อมูลที่หยาบลง · โค้ดทุกบรรทัดในงานนี้จึงเลือกทาง
"ไม่ตอบ" มากกว่าทาง "ตอบใกล้เคียง" เสมอ:

- `_split_plate` **ไม่ซ่อมทะเบียน** แยกได้หรือไม่ได้ เท่านั้น
  เคยคิดจะเติมศูนย์ให้ครบสี่หลัก และแปลงอักษรละตินที่หน้าตาคล้ายอักษรไทย
  ทั้งสองอย่างถูกทิ้งด้วยเหตุผลเดียวกัน: ผลลัพธ์คือทะเบียนที่ดูสมบูรณ์แบบ
  และชี้ไปผิดคัน ซึ่ง **ปลายทางตรวจไม่ได้เลยแม้ถือภาพอยู่ในมือ**
- อ่านไม่ออก = `plate.text` ว่าง + `confidence` เป็น **0 เสมอ** ไม่ว่าโมเดล
  จะบอกมาเท่าไร · ความมั่นใจที่ลอยอยู่โดยไม่มีทะเบียนให้มั่นใจ คือตัวเลขที่
  ปลายทางเอาไปคัดกรองแล้วผิดโดยไม่รู้ตัว
- `plate.text_raw` เก็บสิ่งที่โมเดลอ่านมาดิบๆ ไว้เสมอ **ไม่ใช่ทิ้ง**
  ตอนที่แยกไม่ออก นี่คือสิ่งเดียวที่บอกได้ว่ามันอ่านพลาด หรือเราแยกพลาด
- `summary.plates_unread` **สำคัญกว่า `plates_read`** มันคือตัวเดียวที่บอกว่า
  บริการนี้ใช้ได้จริงแค่ไหนในไซต์นั้น ก่อนจะมี `/truth` · ซ่อนมันเมื่อไหร่
  กราฟจะสวยขึ้นทันทีโดยที่ความจริงไม่ขยับ

## `/truth` กับ `/accuracy` มีตั้งแต่วันแรก ไม่ใช่ค่อยเติมทีหลัง

🔴 **งานนี้วัดความแม่นได้จริง ต่างจากงานอารมณ์และงานสัญชาติของฝั่งคน**
ป้ายทะเบียนมีคำตอบที่ถูกเพียงคำตอบเดียว และคนเปิดภาพดูก็ตัดสินได้ทันที
ไม่เก็บตั้งแต่วันแรก = อีกสามเดือนยังตอบไม่ได้ว่าอ่านถูกกี่เปอร์เซ็นต์
แล้วจะเถียงกันด้วยความรู้สึกแทนตัวเลข

`/accuracy` แยก **อ่านผิด** ออกจาก **อ่านไม่ออก** โดยตั้งใจ สองอย่างนี้แก้คนละวิธี
(ตัวแรกแก้ที่โมเดล/prompt ตัวหลังแก้ที่มุมกล้อง/แสง/ความละเอียด)
และเทียบแบบ **ตรงตัวเป๊ะ ไม่มีใกล้เคียง** ทะเบียนที่ผิดหนึ่งตัวไม่ใช่คำตอบที่ถูก 90%

## ความเป็นส่วนตัว · ข้อที่หนักกว่าสองงานก่อน

ทะเบียนรถชี้ไปหาเจ้าของได้ผ่านฐานทะเบียนกรมขนส่ง ซึ่ง "เสื้อขาวกางเกงดำ" ทำไม่ได้
- ไม่เก็บภาพเป็นค่าเริ่มต้น · `LPR_RETENTION_DAYS` ควรสั้นกว่างานอื่น
- **ห้ามเพิ่มตาราง "รถที่เคยเห็น" หรือ index ที่ทำให้ค้นย้อนหลังด้วยเลขทะเบียนได้ง่าย**
  นั่นคือการสร้างระบบติดตามรถ ซึ่งไม่ใช่สิ่งที่ใครสั่ง และไม่ใช่สิ่งที่บริการนี้เป็น
- ห้าม watchlist ห้าม webhook ห้าม alert · กติกาข้อแรกของทั้ง repo

## ยังไม่ได้วัด ต้องวัดก่อนเชื่อ

- 🔴 **ยังไม่เคยเจอภาพถ่ายป้ายจริงสักใบ** ที่ยิงไปแล้วคือ**ป้ายสังเคราะห์**
  (ดูหัวข้อล่าง) ซึ่งตอบได้แค่ว่าท่อทั้งเส้นทำงาน **ไม่ได้ตอบว่าอ่านภาพจริงได้**
  ภาพจริงมีมุมเอียง แสงสะท้อน ฝุ่น กรอบป้าย และระยะไกล ซึ่งไม่มีในภาพสังเคราะห์เลย
  **งาน OCR ตัวอักษรเป็นคนละความสามารถกับงานบรรยายภาพ** อย่าเดาว่าเก่งเท่ากัน
- คู่อักษรที่หน้าตาคล้ายกัน (ข/ช · ค/ต · บ/ษ) คือที่ที่มันจะพลาด และ prompt
  เตือนเรื่องนี้ไว้แล้ว แต่ **prompt ที่เตือนไว้ ไม่ใช่หลักฐานว่ามันไม่พลาด**
- ต้นทุนต่อภาพ · เดาว่าถูกกว่างานคนเพราะคำตอบสั้นกว่า แต่ยังไม่ได้วัด
- `LPR_MIN_CROP_PX=224` ช่วยความแม่นจริงไหม ยังไม่ได้วัด (เหมือนฝั่งงานคน)
- ยังไม่มี `docs/API.md` ของงานนี้ · ยังไม่มีปลายทางจริงให้ส่ง

## รันตอน dev

```
uvicorn app.main:app --port 8080 --reload
# แล้วเปิด http://127.0.0.1:8080/lpr/verify/
```

45 เทสต์ของงานนี้ไม่ยิงโมเดลจริงสักข้อ · `pytest tests/test_lpr.py -q`

## smoke test บน prod 2026-09-14 · ป้ายสังเคราะห์ ผ่านทั้งเส้น

deploy แล้วยิงป้ายที่เรนเดอร์เอง (TH Sarabun บนพื้นขาว ฉากหลังเทาเข้ม
`1กข 1234` / `สงขลา` ไม่มีตัวรถในภาพ) เข้า `POST /lpr/v1/vehicle` บน prod:

```jsonc
"plate": {"text_raw":"1กข 1234","text":"1กข 1234","prefix":"1",
          "letters":"กข","digits":"1234","color":"ขาว","confidence":0.9},
"province":"สงขลา","province_confidence":0.9,
"vehicle_type":"มองไม่เห็นตัวรถ","vehicle_type_confidence":0.0
```

ถูกทุกช่อง · 10.0 วินาที · และ `vehicle_type` ตอบว่ามองไม่เห็นตัวรถพร้อมความมั่นใจ 0
ซึ่งคือเงื่อนไขที่ Toy เขียนไว้เอง ("กรณีเห็นตัวรถด้วย") ทำงานถูกตั้งแต่รอบแรก

🔴 **อ่านผลนี้ให้ถูก** มันบอกว่า: mount ติด · auth ทำงาน · โมเดลตอบ JSON ตามรูป ·
ตัวแยกทะเบียนแยกถูก · ชุดจังหวัดจับคู่ถูก · ค่าแปลเป็นไทยถูก · ฐานข้อมูลเขียนได้
มันไม่ได้บอกว่า **อ่านป้ายจากภาพถ่ายจริงได้** ป้ายที่เรนเดอร์เองคือป้ายที่คมที่สุด
เท่าที่จะเป็นไปได้ ไม่มีมุม ไม่มีเงา ไม่มีฝุ่น ตัวอักษรเป็นฟอนต์ดิจิทัลไม่ใช่ตัวปั๊ม
**ขั้นต่อไปคือขอภาพถ่ายป้ายจริงจากไซต์ 20-30 ใบ พร้อมทะเบียนที่คนอ่านแล้ว
ยิงเข้า `/truth` ให้ครบ แล้วดู `/v1/accuracy`** ก่อนนั้นความแม่นของบริการนี้คือ
"ไม่รู้" ไม่ใช่ "ดี"
